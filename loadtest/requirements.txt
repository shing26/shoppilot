# 压测与标定脚本的依赖（装在 .venv-loadtest，该目录不入库）。
# 起栈与三条演示不需要这些；只有跑阶梯、SSE 采样、阈值扫描才用。
#   python -m venv .venv-loadtest
#   .venv-loadtest\Scripts\python.exe -m pip install -r loadtest\requirements.txt
locust==2.46.5
psutil==7.2.2
requests==2.34.2
# scripts/calibrate_threshold.py 出图另需 matplotlib；本轮用系统 Python 跑的，
# 没把这个 venv 当成唯一入口，所以只在这里写明，不假装 .venv-loadtest 能一条命令复现曲线图。
