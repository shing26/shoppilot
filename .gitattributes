# docs/interview-qa.md 的换行基线是 CRLF（由 scripts/verify_eval_judge.py 的 EOL_BASELINE 钉住）。
# 对这份文件来说 CR 是行终止符的一部分，不是行尾空白；不写这条，git diff --check 会把每次
# 重新生成产生的合法新增行全部报成 trailing whitespace。
docs/interview-qa.md whitespace=cr-at-eol

# loadtest 产物由压测脚本在本机生成，行尾基线是 CRLF（与已入仓的 0908 产物一致，
# core.autocrlf=false 下按原样入库）；CR 是行终止符的一部分，不写这两条，
# git diff --check 会把整份产物报成 trailing whitespace。
loadtest/results/*.csv whitespace=cr-at-eol
loadtest/results/*.json whitespace=cr-at-eol

# AgentStateMachine.java 的 index 是历史混合行尾（round16 的 apply_patch 遗留），Edit 工具
# 又按 CRLF 写回，任何后续编辑都会把整文件翻成 diff 噪声。text 属性让入库与比对一律按 LF
# 规范化：index 归一为 LF、worktree 的 CRLF 不再进 diff，文件从此 uniform。
shoppilot-gateway/src/main/java/com/shoppilot/gateway/agent/AgentStateMachine.java text

# 三支 eval 脚本的行尾基线钉死 CRLF（verify_eval_judge 的 EOL_BASELINE），但 git diff --check
# 会把新增 CRLF 行报成 trailing whitespace——豁免检查不改变存储，避免每次编辑都出假红。
scripts/run_tool_eval.py whitespace=cr-at-eol
scripts/build_eval_set.py whitespace=cr-at-eol
scripts/verify_eval_judge.py whitespace=cr-at-eol
