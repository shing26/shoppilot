# 30 — 记账收口：README 作用域补全与欠账登记

**What to build:** 把本轮裁决与实测发现的口径一次性写进仓内文档：「可查」作用域成整句、TP99 自己先招饱和条件、74% 的红挂「裁决挂起」标签、指向虚空的票面引用有了真实去向、过期快照带失效标注、工单两条差距进生产就绪清单。全是文字与登记，一行代码不碰，判据一字不动——记账的形状是「红的旁边写为什么红」，不是把红摘掉。

**Blocked by:** 27 — 写回池停机 seam 与饱和计数（饱和口径句要引用已落地的计数事实）；29 — 状态进指标面（指标计数换代与新增名指针）

**Status:** ready-for-agent

- [ ] 否决项段与已知限制段：「转人工落可查工单」补成整句——可查作用域 = 业务 Mock 进程生命周期，看门狗 reseed 与演示世界复位是同一条语义的两端，RESOLVED 之后无回流；定义以 `CONTEXT.md`「可查工单」词条为准，判据原文一字不动
- [ ] 「命中路径 TP99 22ms」旁补「未饱和队列下的读数」标注（饱和自此是票 27 可数的计数事实）
- [ ] 拦截率 74% 那格红补一句「分母 30% 按 ADR 0003 不准入缓存，指标定义与架构决策冲突已知、裁决挂起（ADR 0030 第 4 条）」——红不摘
- [ ] 票 26 S5 的 `aria-disabled`「另一票」补真实去向：登记为下一轮候选（ADR 0030 P1 挂账组），不再指向虚空
- [ ] 未跟踪的 2026-09-13 项目梳理快照顶部加一行失效标注（基线落后一轮、票 23-26 均已落地，读前先对 HEAD）；**不重写全文**，那是历史快照
- [ ] 生产就绪评估追加两条差距：工单跨重启持久化、RESOLVED→买家回流；触发条件引用 ADR 0030 第 2 条
- [ ] ADR 0030 的 P1 挂账清单在 README 侧不自行扩写：语义缓存写回计数器、会话 fail-open 指标、熔断覆盖面、排印组——本轮一字不修，账在 ADR 上
- [ ] 指标名计数校验与换代：按全仓 grep main 现算，README 若引用该数则刷新并写明数法
- [ ] 审计项数保持 95；判据、阈值、gold、评测口径零改动

**Verify:** 文档改动逐条对 ADR 0030 与 `CONTEXT.md` 复核措辞 -> 收口审计复跑 95 项全绿 -> 门禁全绿（零额度）。

*** Add File: D:/ShopPilot/.scratch/shoppilot-mvp/issues/31-crash-attribution-and-heap-caps.md
# 31 — JVM 崩溃归因与峰值条件入证据

**What to build:** README 说「凌晨两次压测后网关凭空消失、没有 hs_err、归因未定」——而 14 份崩溃日志一直躺在仓库根。本票把它们读完，把「归因未定」改成有因结论；把头条吞吐数字的峰值步主机条件从「已知限制」提到数字旁边；给两个 JVM 的起栈脚本设显式堆上限，并明写历轮读数的供法。全票是证据与运维参数，不改任何判据与压测逻辑。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] 读 14 份 `hs_err_pid*.log`（连同 replay 日志）出归因表：崩溃类型（native 内存 OOM）、时间簇、网关与业务 Mock 各自的份、崩溃瞬间系统空闲内存区间；表述以日志原文为据，引用日志文件名
- [ ] README 技术债「归因未定」那条改有因结论；「没有 hs_err_pid 日志」的字样在仓内不得再出现
- [ ] 三个 headline QPS 数字旁补峰值步主机条件（压测 env 产物里的空闲内存与 CPU 读数），引用对应 env-json 产物名，标注「登记当时状态，不作跨轮证据」
- [ ] 业务 Mock 与网关的起栈脚本显式设 `-XX:MaxRAMPercentage`；看门狗抬堆的既有逻辑与新参数对齐，不留两套打架
- [ ] README 运维段注一句：起栈形态自此带显式堆上限，历轮压测读数按当时状态原样供着，不承诺跨形态复现
- [ ] 机器复跑判据三条：README 全文 grep 无「没有 hs_err」式字样；归因段至少引用一份日志文件名；起栈脚本含堆上限参数
- [ ] 审计项数保持 95；判据、阈值、压测判定逻辑一字不动；零额度

**Verify:** 归因表逐行对日志原文抽查 -> 三条机器判据各跑一遍 -> 起栈脚本改动在本机目测一次启动参数生效（活体只登记当时状态）-> 门禁全绿。
