# 18 — 大促压测：双曲线、虚拟线程对比与节约率基线

**What to build:** 两组流量模型的 QPS 拐点与 TP99、一组虚拟线程开关对比、一次关缓存基线跑测，产出全部对外宣称数字的原始数据。落实 ADR 0001、0011。

**Blocked by:** 14 — 降级与工单与 local 验证；15 — 单文件调试台；16 — Tool Calling 标注评测集

**Status:** ready-for-agent

**Verify:** 依次跑双曲线、虚拟线程开关对比、关缓存基线三组 -> 每组结果附带 CPU/堆/版本等环境记录，重跑一次数字在同一量级内可复现。

- [ ] `perf` 模式 `MockLLMClient` 固定延迟可配（默认首字 300ms、总时长 500ms），并支持注入超时用于降级演练
- [ ] 流量模型两条曲线：L1 主导（55% 文本重复热点 + 15% 口语改写 + 15% 业务办理 + 15% 长尾 RAG）与 L2 主导
- [ ] QPS/TP99 压同步端点；SSE 端点单独测 500 并发长连接下的 TTFT 与堆内存占用，两类指标不得混报
- [ ] 阶梯 50 -> 100 -> 200 -> 400 -> 800 -> 1200 并发，记录 QPS、TP99、错误率、双口径拦截率，找"QPS 不再上升而 TP99 陡增"的拐点
- [ ] 虚拟线程对比实验：`spring.threads.virtual.enabled=false` + 200 平台线程池 vs 开启，同一流量模型两组数据
- [ ] Token 节约率：`cache.enabled=false` 重放同一 trace 得基线，再与开启缓存的消耗对比
- [ ] 压测每请求生成新幂等 token，否则写操作全被判重复提交
- [ ] 记录 HikariCP 连接池饱和点（默认 10 连接会先于 CPU 饱和），调池后复测并写明前后差异
- [ ] 压测期间 Ollama 停用；每轮结果与运行环境（CPU、堆、版本）一起落盘，保证可复现

## Handoff notes

（收尾时填写：关键决策 + 你需要能当场回答的三个追问）
