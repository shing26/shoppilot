# 09 — L2 语义缓存、纪元失效与跨实例合并

**What to build:** 口语化改写的政策问题在文本不同的情况下仍被语义缓存拦下；政策一改，旧答案整纪元作废；两个实例同时收到同一新问题时只穿透一次。

**Blocked by:** 06 — L1 精确缓存、写回资格与进程内 singleflight；07 — 意图三级级联判定补全

**Status:** ready-for-agent

**Verify:** 改写句二次提问 -> 命中 L2；推进知识库纪元后再问 -> 不命中；换租户身份问同一句 -> 不命中。

- [ ] 精确缓存 miss 后才向量化，直接复用 ticket 07 落地的 embedding 客户端与已定案的 `intent`，零临时代码、零重复 embedding 调用
- [ ] Qdrant 缓存集合检索带 payload filter：`tenant_id` + `scope` + `intent` + `kb_epoch`，cosine >= 0.95 判定命中；意图过滤缺失视为实现缺陷而非性能优化项
- [ ] 缓存条目写入时同步落 Qdrant 向量与 Redis 正文，两处缺一即视为未写入（不得出现向量在、正文丢的半状态）
- [ ] `kb_epoch` 推进后旧纪元条目不再被读取，且由后台清理任务删除，不靠 TTL 自然过期
- [ ] 跨实例合并：Redis `SETNX` 短锁 + 结果回读，与进程内层组合成两层，锁必须有 TTL 不得死锁
- [ ] 指标：L1 命中数、L2 命中数、准入数、穿透数分别上报，供 ticket 18 计算双口径拦截率
- [ ] 用例：同意图改写句命中 L2；跨租户不命中；纪元推进后不命中

## Handoff notes

（收尾时填写：关键决策 + 你需要能当场回答的三个追问）
