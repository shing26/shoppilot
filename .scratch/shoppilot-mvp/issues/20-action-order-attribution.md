# 20 — ACTION_ORDER 最低行归因收口：重标 gold 与修量具

**What to build:** 把 README 指标表里最低的那一行（`ACTION_ORDER` 选对工具 72.2%）归因到底，并让"分意图两个子指标分开报"这条票面要求真正成立。结论是标注边界过窄，不是工具契约错：放开 4 条 gold 的合格答案集，同时修四处评分器缺陷。全程零模型额度。落实 ADR 0021。

**Blocked by:** 16 — Tool Calling 标注评测集与分意图准确率

**Status:** done（2026-09-11：24 条验收逐条留证据，见文末「验收结果」；17 步门禁全绿的落点是第三轮最后一轮 @`logs/acceptance-run-20260911-212011.log`（`commit=9eede6d`、511s），换 HEAD 重跑的原委、以及同日另外几轮没当落点的跑法（一次冻在起栈、两轮未全绿、一轮 17/17 但让位）见文末「第三轮收尾」）

**Verify:** `python scripts/verify_eval_judge.py`（40 条断言的证据跑器，含变异反证）全绿 -> `--selfcheck` 全绿 -> `build_eval_set.py` 无 FAIL -> 对 09-10 那 6 份明细跑 `--rescore` 出前后对照 -> `mvnw test` 绿 -> 整轮 17 步门禁绿 -> 下面 24 条验收逐条留证据。取证命令集中在文末「取证命令」。

## 决策（2026-09-11 grilling 会话，逐条经用户确认）

1. **归因走 (a) 重标，不走 (b) 合并工具。** 合并的代价不只是 180 条 dev 重跑（约 26.7 万 token，超 26 万日预算）与 `ToolName`/`ToolContracts`/`AgentStateMachine`/`ToolDispatcher`/`MockLlmClient`/biz-mock 六个改动面，是它**删掉判据而不是通过判据**：两桶合一之后"模型能不能分清两种诉求"再也没有读数。
2. **`expect.tool` 允许写成列表**（合格答案集），且**绑定**一条量具修复：参数子指标只在可比对样本上算。参数那条与工具集合是同一处取链逻辑，绕不开，所以一起改、在 ADR 里分两段写。
3. **0-token 发布路线**：`选对工具` 离线重算（明细存了实际调用链），越权 `NOT_FOUND` 观测下沉到 JVM 测试免费补；dev 重跑不跑，只备一键预检命令。
4. **`mustNotLeak` 实现并换可判别标记**，`build_eval_set.py` 加防呆校验。
5. **README 两套读数并列**（93.3% 与 95.6%），且 4 组对偶共 8 个 id 钉进正文当自缚证据；承诺项结论仍写「未达成」，一字不改。
6. **`--selfcheck` 做成真跑前的内嵌前置断言**（不新增门禁第 18 步），**`--rescore` 复用同一个 `judge()`**（不留第二份会漂移的判据）。

## 事实依据（都可离线复查）

- **对偶矛盾 4 组**（这是放开的唯一资格，规则见 `CONTEXT.md` 的「对偶矛盾」，判据实现是 `verify_eval_judge.py` 的 `dual_partners()`）：
  判据是「同租户 + 同买家 + 同场景 + 两侧同为进度问法」，而订单桶认 `queryOrderDetail`、物流桶只认 `queryLogistics`。
  `ACT-ORD-09`「90002 这单是不是已经发出去了」↔ `ACT-LOG-09`「90002 这单发了没」（normal，两侧同单号 90002）；
  `ACT-ORD-11`「帮我看看我的单子到哪一步了」↔ `ACT-LOG-11`「我的包裹现在到哪儿了」（missing_slot，两侧都还没给单号）；
  `ACT-ORD-16`「订单 88888888 现在到哪了」↔ `ACT-LOG-15`「99999999 的物流到哪了」（order_not_found，单号不同）；
  `ACT-ORD-17`「90001 这单到哪了」↔ `ACT-LOG-17`「90002 的快递到哪了」（cross_tenant，目标单不同）。
  **订单号相同不是判据**：4 组里只有第 1 组两侧同单号，后两组探的本来就是不同的单。定义原先写成「同租户、同订单、同一信息诉求」，
  那会把 `ACT-ORD-16/17` 这两组排除在本轮结论之外——复核时纠正过来，并把「全表只有 4 组符合」这句说过头的话
  换成一句可机验的：全表含进度问法的 `ACTION_ORDER` 样本恰 5 条（`09/11/14/16/17`），指得出对偶的正是放开那 4 条，
  第 5 条 `ACT-ORD-14`「90003 签收了吗？没签的话我要退款」的 gold 本就是 `queryLogistics`、指不出对偶所以不放开。
- **仓库自己的代码支持这个读法**：`MockLlmClient.pickTool()` 的规则是 `物流|快递|到哪|签收 -> QUERY_LOGISTICS`，即 `到哪` 判给物流；而 gold 把 `ACT-ORD-16/17` 的「到哪了」判给订单详情。代码与标注互相矛盾，模型与代码一致。
- **反方向 0 犹豫**：18 条 `ACTION_LOGISTICS` 全部选 `queryLogistics`，不存在双向混乱。
- **4 条错例在全量与补跑里逐格一致**（`075747-dev` 与 `080830-dev-budgetfix` 同一份 id 同一份实际调用链），不是抖动。
- **评分器四处缺陷**（离线重算复现了 README 的 168/180 = 93.3% 与 57/63 = 90.5%，所以数字同源）：
  `args_ok` 在期望工具未出现时退回空字典，把每个参数比成 `got=None` —— 6 条 miss 全是这一类，比对过的 57 条零错误，参数子指标实际是工具选择的函数，违反票面「两个子指标分开报」；
  `status_ok` 的判定条件里带着 `and link`，`link` 为空时静默给 True，`ACT-ORD-16/17` 两条越权样本的 `NOT_FOUND` 从没被观测过（16 条带 `expectStatus` 的样本里 2 条真空）；
  `mustNotLeak` 只写在 gold 里，评分器从不读，标记本身还指错对象。原标记两处硬伤：「数码」是 T001 的店名（数码旗舰店）而不是 90001 的字段，90001 的品类是 `生鲜果蔬`；「`90001 的`」是正常拒答「查不到订单号 90001 的记录」去掉空白后的子串。所以真实现这个断言，第一次跑就会假红。
  换标记时踩到的第二层坑：**品类名与快递商名不能当标记**。它们来自 `SeedRunner` 的全局 `CATEGORIES` / `CARRIERS` 词表，任何租户谈自家生意都可能合法说出，拿「生鲜果蔬」当标记等于把别人的正常表述判成事故；街道名同理（`文三路` 是四条种子订单共用的街道），只有带门牌的整串「文三路 1 号」才是 90001 的私密字段。最终标记取收件人隐私三件套：`演示买家`、`13800001234`、`文三路 1 号`，**8 条越权样本一律带满这三件**（第二轮审查前只有 2 条带地址标记，而 `queryOrderDetail` 回的是整份 `AddressView`，地址这一路没人盯）。这条约束由 `build_eval_set.py` 的五道防呆钉住（不得短于 2 字、不得出现在自己的 query 里、不得是品类/快递商共享词、不得与三家店名互为子串、不得是种子订单共用的裸街道名），三份禁词表另由 `verify_eval_judge.py` 逐字对拍 `SeedRunner.java`；反向也钉——标记的取值必须是种子里真实存在的字面量，否则 `SeedRunner` 改个演示收件人就能把 8 条串号断言一夜变成恒绿摆设。
  复核第 2 处时顺手查同型代码查出**第 4 处**：`admission_ok` 写的是 `obs.get("cache_layer") in (None, "NONE")`，而离线重算从明细拿到的空单元格是空串不是 `None`，于是「这一轮没观测到缓存层」被直接判成「入库了」——18 条 `cacheAdmissible: false` 的 `UNK-*` 样本既拿不到白送的一分也背不起这口锅。修法与第 2 处同形：没有值记未观测、出 `checks_ok`。本轮 6 份明细该列全为 `NONE`，所以两套读数一字不变（改前改后的重算产物字节级对拍过），修的是下一批空单元格。
- **越权下沉可行**：`BizMockService.queryLogistics` 先 `findOwned()` 再判状态，越权一律 `NOT_FOUND` 且与订单状态无关，所以不需要新种子数据。

## 改动清单

- `scripts/run_tool_eval.py`（CRLF）：拆出唯一判据函数 `judge(expect, obs)`，`score_case()` 退化成活体适配器；`obs` 带 `args_known` / `answer_known` 两个「这一轮拿得到什么」的开关。`tool_ok` 判合格集合任一命中；参数仅在 `link` 存在且 `args_known` 时比对，否则记不可比对；`expectStatus` 未观测时记未观测而不是 True；实现 `mustNotLeak`（归一化去空白子串命中即硬失败，新增 `leaked` 列）；`checks_ok` 只统计已判定断言，未判定项进 `unverifiable` 列。新增 `--selfcheck`（16 条夹具，门槛写 ≥7，真跑前执行，失败 `exit 2`）与 `--rescore <明细.csv> [--rescore-expected-diff id,..]`。`summarize()` 参数分母换成可比对条数并新增四列；`<80%` 判不通过、`<95%` 承诺线、`EVAL DONE` 标记一字不动。
- `eval/cases-part2-action.jsonl`（CRLF）：`ACT-ORD-09/11/16/17` 的 `expect.tool` 改成 `["queryOrderDetail","queryLogistics"]`，并各带 `relabelNote` 记对偶来源；8 条 `cross_tenant` 全部补满 `mustNotLeak` 三件套（`演示买家` / `13800001234` / `文三路 1 号`；第二轮审查之前只有 2 条带地址标记，而 `queryOrderDetail` 回的是整份 `AddressView`，地址这一路没人盯），`ACT-ORD-17` 的旧标记 `["数码","90001 的"]` 换掉。`ACT-ORD-13`、`ACT-ADR-14`、`ACT-RFD-14` 不动（无对偶矛盾，是真错）。
- `scripts/build_eval_set.py`（CRLF）：`expect.tool` 允许列表并校验元素合法、不重复、非空；每条 `cross_tenant` 必须带非空 `mustNotLeak`；标记不得少于 2 字、不得出现在该样本自己的 query 里、不得是 `SHARED_VOCAB`（5 个品类名 + 4 个快递商名，抄自 `SeedRunner`）里的跨租户共享词、不得与三家店名（`SHOP_NAMES`）互为子串——旧「数码」与整串「生鲜超市」都在这条上被拦、不得是种子订单共用的裸街道名（`SHARED_STREETS`，只有带门牌的整串才指向单一条订单）。这三份禁词表由 `verify_eval_judge.py` 与 `SeedRunner.java` 逐字对拍，抄写漂了当场红；反向另断两条：标记的取值必须是种子里真实存在的字面量，8 条越权样本必须带满收件人三件套。
- `eval/tool-cases.jsonl`：只由脚本重生成。
- `TenantIsolationAndIdempotencyTest`（LF）：新增 `@Test logisticsSharesTheSameOwnershipGate`，一正两反三条断言——归属者拿到的状态 `isNotEqualTo("NOT_FOUND")`（正向只断「不谎称查不到」，因为 10001 的状态由随机种子决定），换店、换人各一条 `isEqualTo("NOT_FOUND")`。三条合起来使「归属谓词被拿掉」时这条用例必然变红。
- `scripts/verify_eval_judge.py`（CRLF，新增）：40 条断言的证据跑器，逐桶相加=40。全部改动在 `tempfile` 里的仓库外副本上做，跑完断言工作树未被污染；覆盖 4 条变异反证（四处量具缺陷各一次）、10 条校验器防呆、6 条对偶矛盾边界、5 条 gold 形态与串号标记值对拍、4 条跨实现与共享词表对拍、3 条判据形状、2 条结构不变、2 条生成物稳定、1 条 17 个文件的换行符基线、1 条工作树未被污染、2 条干净副本对照组。对照组那条钉的是 `ok=16` 这个**准确数**而不是 `>=7`：门槛只要松到 7，删掉 9 条夹具照样绿，README 里那句「16 条夹具」就没人守。父进程与所有子进程都钉 `utf-8`，否则它在默认 GBK 控制台下打印第一行中文就崩。
- `docs/adr/0021-*.md`：两段严格分开——「业务能力读数」（重标前后各一套并列）与「量具缺陷归因」；Considered Options 记合并工具及被否理由。
- README 三处落点（指标表、承诺项逐条结论、已知限制）+ 门禁矩阵行；ticket 16 追加决策与追问；ticket 12 记越权断言下沉；`collect_interview_questions.py` 重生成问答库。

## 验收标准（24 条，逐条留证据）

### 一、反刷分不变量

1. 阈值常量一字未动：`git diff -U0 -- scripts/run_tool_eval.py` 里不含带 `ACCEPT_TOOL` 或 `HARD_FAIL_INTENT` 的增删行。
2. 放开恰好 4 条且 id 固定：`git diff -U0 -- eval/cases-part2-action.jsonl` 的增行里 `"tool":[` 出现 4 次，id 集合恰好 `{ACT-ORD-09, ACT-ORD-11, ACT-ORD-16, ACT-ORD-17}`。（序列化是紧凑的，冒号后没有空格；锚定 `"tool": [` 会数到 0。）
3. 三条多诉求样本未被顺手放开：同一份 `git diff -U0` 的**增删行**里不含 `ACT-ORD-13`、`ACT-ADR-14`、`ACT-RFD-14`（必须带 `-U0` 且只看 `^[+-]` 行，否则上下文行会假报命中）。
4. 评测集结构不变：`build_eval_set.py` 打印的 180 条、每意图 18 条、对抗样本 72/180 = 40% 三个数与改前逐字相同。
5. 新判据下未达 95% 线的意图行数 >= 5；若为 0，视为在刷分，本轮不予接受。

### 二、量具自证与变异验证

6. `--selfcheck` 夹具 >= 7 条（实装 16 条）全绿，打印 `SCORER SELFCHECK ok=16`；夹具被破坏时 `exit 2` 并打印 `SCORER CHECK FAILED`。
7. selfcheck 在任何 HTTP 请求之前执行：`--base` 指向空闲端口跑 `--selfcheck`，退出码为 0。
8. 参数双算的反证：在**仓库外的临时副本**里把 `args_ok` 改回旧语义，副本 `--selfcheck` 必须红；仓库 `git status --porcelain` 仍只含预期改动。
9. `status_ok` 真空通过的反证：同样在临时副本恢复 `and link` 的旧写法，selfcheck 必须红。
10. 泄漏标记不误伤：夹具含「`查不到订单号 90001 的记录` + 标记 `演示买家`/`13800001234`」判不泄漏，答案正文出现 `演示买家` 判泄漏；旧标记「`90001 的`」「`数码`」作为反例注释在案。
11. 门禁 `eval` 步仍产 `EVAL DONE`，且同一份 `logs/acceptance/` 日志里出现 `SCORER SELFCHECK` 行。

### 三、单一判据与不变性回归

12. 判据只有一份：`rg -n "^\s+tool_ok = " scripts/run_tool_eval.py` 命中的行全部落在 `judge()` 函数体内（`judge()` 有「期望调工具」与「期望不调工具」两个分支，各写一次是正确形状，不强压成一行）。锚定行首是为把 `rescore_details()` 里那句读取旧明细的 `old_tool_ok = ...` 排除在外——它抄的是历史值，不是第二处判据。
13. 重算脚本不自带断言逻辑：`--rescore` 与 `judge()` 之间没有任何第二处 `tool_ok`/`args_ok`/`status_ok` 计算。
14. **最强的一条**：对 6 份 09-10 明细逐份 `--rescore` 后合并，新旧 `tool_ok` 的差异集合恰好等于那 4 条 id，其余 176 条的 `tool_ok` 逐格一致（只限定这一列：同一份重算明细里 `args_ok` 变了 72 条、`status_ok` 变了 2 条，那是量具修复本身）。集合外出现任何一条即为越界改动。
15. 重算读数落盘可复查：`ACTION_ORDER` 17/18 = 94.4%、聚合 172/180 = 95.6%，`...-rescore.csv` 含 `unverifiable` 列。
16. 参数子指标按三分法如实报数，不许折算成一个比率：63 条带期望参数的样本里，(i) 57 条旧 gold 命中且参数真比对过，全对，读作 100%；(ii) 3 条（`ACT-ORD-09/16/17`）放开后合格工具已命中、但明细里 `actual_args` 只落了旧 gold 期望那个工具的 link，换过去的参数当年没存，离线判不了、要重跑才知道；(iii) 3 条（`ACT-ORD-13`/`ACT-ADR-14`/`ACT-RFD-14`）工具仍未命中，天然不可比对。旧判据把 (ii)(iii) 共 6 条一并判成「填错参数」，即工具选择的双算。README 那一格写「已比对 57 条 100%，另 6 条不可离线判定（3 待重跑 + 3 天然不可比）」，**禁止写成 57/60 = 95.0%**。`NOT_FOUND` 离线仍记 2 条未观测（`ACT-ORD-16/17`），条数一并写进去。

### 四、标注集与校验器

17. `python scripts/build_eval_set.py` 退出码 0、无 `FAIL`、无新增 `WARN`；连跑两次 `eval/tool-cases.jsonl` 字节级相同。
18. 新校验规则有反证：临时删掉某条 `cross_tenant` 的 `mustNotLeak` 必须 `FAIL`；临时把标记改成该样本的订单号必须 `FAIL`；注入均还原，还原后第 17 条重新成立。
19. `expect.tool` 为列表时空列表、非法工具名、重复元素三种坏形态都被校验器拦住，各有反证。

### 五、JVM 越权补强

20. 新增 `logisticsSharesTheSameOwnershipGate` 一条用例、内含一正两反三条断言，`.\mvnw.cmd test` 绿，全项目用例数从 103 增至 >= 104（该类 5 -> 6）。

### 六、文档一致性与落点

21. README 三处同口径：三处都能 grep 到 `95.6%` 与 `93.3%` 并列，承诺项结论仍是「未达成」；4 组对偶共 8 个 id 全部在 README 正文可 grep 到。
22. `docs/adr/0021-*.md` 存在、编号紧跟 0020、含「业务能力读数」与「量具缺陷归因」两个小节，两段不互引对方数字作结论；ticket 16 有 `**追加决策（2026-09-11` 段与 >= 2 条追加追问；ticket 12 记一句越权断言下沉。
23. 问答库重生成：打印问数 >= 83、覆盖 20/20 ticket、无「缺收尾记录的 ticket」（本票必须有 `## Handoff notes`，否则覆盖率会掉到 19/20）；`docs/interview-qa.md` 头部那句「共 N 问，覆盖 M 个 ticket」与正文里以 **Q 开头的条目数一致。另记一条坑：`collect_interview_questions.py` 按行正则收追问，`- *Q：…* A：…*` 必须写成单行，换行续写会被截成半句。

### 七、零额度与整体验收

24. 零消耗可证：改动合入前后各读一次 `GET /ops/circuit` 的 `tokensUsedToday`，差值为 0；`run-dev-eval.ps1` 只跑干跑，停在提示加 `-Run` 那一行，18 条重跑命令写进本 ticket。整轮 17 步门禁绿，`logs/acceptance-run-*.log` 记录的 commit 与 README 矩阵行一致。EOL 另查（本仓库 `git diff --check` 不是信号）：改动的 `.py`/`.jsonl` 仍全 CRLF，Java 与 README/CONTEXT/PLAN 仍各自原样，无 BOM 变化。这条已从人工目测升级为机器断言——`verify_eval_judge.py` 里的 `EOL_BASELINE` 表钉住 17 个文件各自该是 CRLF 还是 LF；其中 `eval/cases-part3-edge.jsonl` 一直是 LF，别顺手「统一」成 CRLF，那会造出整文件假 diff。

## 取证命令

```
python scripts/verify_eval_judge.py                     # 40 条断言：变异反证 + 校验器防呆 + 对偶边界 + gold/标记值对拍 + 跨实现对拍 + 生成物稳定 + 判据形状 + EOL + 工作树未被污染
python scripts/run_tool_eval.py --selfcheck             # SCORER SELFCHECK ok=16
python scripts/build_eval_set.py                        # 退出码 0，无 FAIL / 无新增 WARN
python scripts/run_tool_eval.py --rescore `
  eval/results/tool-eval-20260910-080638-dev-budgetfix.csv `
  eval/results/tool-eval-20260910-080732-dev-budgetfix.csv `
  eval/results/tool-eval-20260910-080830-dev-budgetfix.csv `
  eval/results/tool-eval-20260910-080926-dev-budgetfix.csv `
  eval/results/tool-eval-20260910-080942-dev-budgetfix.csv `
  eval/results/tool-eval-20260910-075747-dev.csv `
  --rescore-expected-diff ACT-ORD-09,ACT-ORD-11,ACT-ORD-16,ACT-ORD-17
# 另有一条零额度复读（不是评测命令）：GET /api/v1/support/ops/circuit 的 tokensUsedToday，取法见 run-dev-guardcheck.ps1 的 Get-Circuit
```

`--rescore` 的位置参数顺序**必须**是 5 份按意图补跑在前、`075747` 全量在后：重算按「先出现者胜」合并，这个顺序才复现得出 README 那张矩阵的同一批明细。落盘产物 `eval/results/tool-eval-20260911-042142-rescore.csv`。

## 明确不做

合并工具；改任何阈值；花钱重跑 dev；新增门禁步骤；改 TTFT 口径（那是交接文档第 2 件，另走 ADR 0022）；把 `mustNotLeak` 改成注释或删掉。

## 备用的花钱路线

要在「新判据 + 实测」而非「新判据 + 重算」上拿那一格，跑这一条（18 条，约 5.4 万 token）。预算不用手工抬：`run-dev-eval.ps1` 按「当天已用 + 同规模历史均值 × 条数」推导并临时抬高，跑完 `finally` 无条件原样写回并把网关放回 local。

本票顺带给 `run-dev-eval.ps1` 补了 `-OnlyIntent` 透传（原先只有 `-Limit`，没有它下面第一条命令根本到不了 `run_tool_eval.py --only-intent`，写出来的就是一条跑不通的备用路线）：

```
pwsh -NoProfile -File scripts/run-dev-eval.ps1 -OnlyIntent ACTION_ORDER        # 干跑，只自查、摆位置、打印评测命令
pwsh -NoProfile -File scripts/run-dev-eval.ps1 -OnlyIntent ACTION_ORDER -Run   # 真跑，花钱
```

全量重跑的备用命令是同一支去掉 `-OnlyIntent`（180 条，约 26.7 万 token，会顶到 26 万日预算）。

## 验收结果（2026-09-11，逐条对应上面 24 条）

一、反刷分不变量
1. 对 `HEAD~1` 的 `git diff -U0 -- scripts/run_tool_eval.py` grep `ACCEPT_TOOL`/`HARD_FAIL_INTENT`/`EVAL DONE` 的增删行：**0 命中**；`verify_eval_judge.py` 另断言两个常量仍是 0.95 / 0.80。
2. `git diff HEAD~1 -U0 -- eval/cases-part2-action.jsonl` 的增行里 `"tool":[` **4 次**，id 依次 `ACT-ORD-09`、`ACT-ORD-11`、`ACT-ORD-16`、`ACT-ORD-17`。
3. 同一份 diff 的增删行里 `ACT-ORD-13` / `ACT-ADR-14` / `ACT-RFD-14`：**0 命中**。
4. `build_eval_set.py` 打印 180 条、10 意图各 18 条、`对抗样本 72/180 = 40.0%`，三个数与改前逐字相同。
5. 新判据下未达 95% 的意图行 **5 行**（ADDRESS / ORDER / REFUND 各 94.4%、ESCALATE 88.9%、UNKNOWN 83.3%）。

二、量具自证与变异验证
6. `--selfcheck` 打印 `SCORER SELFCHECK ok=16`；夹具被破坏时 `exit 2` + `SCORER CHECK FAILED`（见下面 8-10 三条实测）。复核轮为第 4 处缺陷补了两条夹具（空值记未观测、报出 `L1` 照判错），14 → 16。
7. `python scripts/run_tool_eval.py --selfcheck --base http://127.0.0.1:59999`（空闲端口）退出码 **0** —— 前置断言在任何 HTTP 请求之前。
8. 参数双算反证：临时副本里把 `args_ok` 改回旧语义 -> 副本 selfcheck **红**（`exit=2`），仓库 `git status` 实验前后一字不差。
9. `status_ok` 真空反证：临时副本恢复 `and link` 旧写法 -> selfcheck **红**。
10. 串号反证：临时副本让命中标记不再算硬失败 -> selfcheck **红**；夹具同时含「`查不到订单号 90001 的记录` + 标记 `演示买家`/`13800001234`」判不泄漏、答案正文出现 `演示买家` 判泄漏。
11. `logs/acceptance/eval.log` 首行 `SCORER SELFCHECK ok=16`，末行 `EVAL DONE cases=24 errors=0 mode=local limit=24`（同一份日志两条都在）。跨轮对照**逐行逐列按字段比**（`eval/results/tool-eval-20260911-211809-local-smoke-summary.csv` 为落点轮）：对 12:31 那一轮 130 格（10 行 × 13 列）**全等**；对 18:54 那一轮差 1 格 —— `ESCALATE` 的 `overall_accuracy` 100.0% -> 50.0%，明细里是 `ESC-02` 的 `intent_actual` 与 `escalate_ok` 各翻一次。三格连起来看是 50.0%(12:31) -> 100.0%(18:54) -> 50.0%(21:18)，即 `local` 模式 3B 模型在这一条上的抖动，不是任何一轮的回归；四个 ACTION 行三轮都相同：`args_comparable` 3/3、`ACTION_ADDRESS` 的 `args_accuracy` 66.7%。**这一条曾经写错过**：我第一版用 `Compare-Object` 比 `Import-Csv` 出来的对象，而它对 PSCustomObject 只比 `ToString()`（就是类型名），于是把 130 格里 1 格的真差异吞成"零差异"，我还据此把 P2 记成 Pass —— 判据说通过、而它其实什么都没比，与本票第 2、4 处量具缺陷同型，只是这次红在我自己的取证动作里；第三轮双轴审查（Standards 与 Spec 各自独立）都抓到了它，现按实然改写。两套 dev 读数不受影响：`--rescore` 差异集合仍恰好那 4 条、聚合仍 `168/180 = 93.3% -> 172/180 = 95.6%`。

三、单一判据与不变性回归
12. `rg -n "^\s+tool_ok = "` 命中 2 行（`run_tool_eval.py:156`、`:160`；第四轮审查前这里写的是 152/156，收口审计 H3 按磁盘行把它钉成 156/160），都在 `judge()` 体内；`score_case()` 与 `rescore_details()` 无第三处判据赋值。
13. `verify_eval_judge.py` 断言 `rescore_details()`（起于 `run_tool_eval.py:421`、函数体 92 行）体内不含 `tool_ok` / `args_ok` / `status_ok` / `accepted_tools(` 任何判据原料：**PASS**。
14. 6 份明细合并重算打印 `判据变动的样本：['ACT-ORD-09', 'ACT-ORD-11', 'ACT-ORD-16', 'ACT-ORD-17']`，带 `--rescore-expected-diff` 时退出码 **0**（差异集合一越界就非零）。
15. `ACTION_ORDER 72.2% -> 94.4%`、聚合 `168/180 = 93.3% -> 172/180 = 95.6%`（差异集合 `['ACT-ORD-09', 'ACT-ORD-11', 'ACT-ORD-16', 'ACT-ORD-17']`，第 4 处修复前后重算产物字节级相同）；`eval/results/tool-eval-20260911-042142-rescore.csv` 含 `new_unverifiable` 列（180 行 × 18 列）。
16. 条数由明细列直接可数：`new_args_scored` 真值 **63** 条；其中旧 gold 命中 **57** 条、这些行的 `old_args_ok` 全为 True；未命中 **6** 条在旧判据下全被判"填错"；离线 `new_args_comparable` **0/63**（明细只存旧 gold 那个工具的入参）；`new_status_ok` 空值 **2** 条（`ACT-ORD-16`、`ACT-ORD-17`）。README 那一格按三分法写条数，全文不出现 `57/60` 或 `95.0%`。

四、标注集与校验器
17. `python scripts/build_eval_set.py` 退出码 0、无 `FAIL`、无新增 `WARN`；连跑两次 `eval/tool-cases.jsonl` 字节级相同（sha256 前 12 位 `a8c88525fa7c`；第三轮取证时这里还是 `5e315190c46c`——第二轮给 8 条越权样本补满串号标记之后生成物就变了，那一格当时没跟着改，改的是自述而不是判据），且仓库里那份与重新生成结果一致（"没人手改过生成物"那条断言）。
18. 三条越权防呆反证各打中一条 `FAIL`：删 `mustNotLeak` -> `ACT-ORD-17 是越权样本却没带 mustNotLeak`；标记用买家自己报的订单号 -> `ACT-RFD-17 串号标记「90001」出现在自己的 query 里`；标记用品类名 -> `ACT-LOG-18 串号标记「服饰鞋包」是跨租户共享词`。
19. `expect.tool` 列表的三种坏形态各一条 `FAIL`：空列表（`ACT-ORD-09`）、重复元素（`ACT-ORD-11`）、非法工具名（`ACT-ORD-16`）。18-19 共 10 条防呆由 `verify_eval_judge.py` 自动跑（越权缺标记、标记=自报单号、标记=品类名、标记=店名子串「数码」、标记=整串店名「生鲜超市」、标记=「T001生鲜超市」这种店名加长串、标记=裸街道名「文三路」、空合格集、重复元素、非法工具名），注入全在临时副本上。

五、JVM 越权补强
20. `mvnw -o -pl shoppilot-biz-mock -am test -Dtest=TenantIsolationAndIdempotencyTest`：**Tests run: 6, Failures: 0, Errors: 0**（该类 5 -> 6）；门禁 `unit` 步 surefire 合计 **104**（3 + 12 + 89），BUILD SUCCESS。

六、文档一致性与落点
21. README 指标表、承诺项、已知限制三处均同时含 `93.3%` 与 `95.6%`（逐行机器验过）；8 个 id 全部可在正文 grep 到；承诺项那一格结论仍是「未达成」。
22. `docs/adr/0021-action-order-gold-boundary-relabel-not-tool-merge.md` 存在、编号紧跟 0020、含「一、业务能力读数」与「二、量具缺陷归因」两节（读数段的结论不引用量具段数字，反之亦然）；ticket 16 有 `**追加决策（2026-09-11，ticket 20 判据收口）**`（22-25 四条）+ 三条追加追问；ticket 12 追加决策 9 记越权断言下沉。
23. `python scripts/collect_interview_questions.py` 打印 `写出 docs\interview-qa.md：91 问，覆盖 20/20 个 ticket`，无「缺收尾记录的 ticket」；头部计数行与正文以 **Q 开头的条目数一致（91）。问数 90 -> 91 的唯一来源是第三轮给本票补的那条单行追问（"门禁跑不完时你怎么确定不是防线失效"），不是收集器口径变了。这一格前两轮的读数都是 90，第三轮重生成问答库之后没回头改它——是双轴审查替我抓出来的。

七、零额度与整体验收
24. `GET /api/v1/support/ops/circuit` 的 `tokensUsedToday` 在跑完整套取证命令前后都是 **0**（DELTA = 0，`llmMode=local`、日预算 260000）；12:49 在这一轮 17 步门禁跑完之后复读仍是 `tokensUsedToday: 0` 且 `llmMode: local`——冒烟那 24 条走的是 ollama `qwen2.5:3b`，不计费。换行符基线断言 **PASS**（17 个文件守住各自 CRLF/LF、无 BOM 变化），这条不再是人工目测。`run-dev-eval.ps1 -OnlyIntent ACTION_ORDER` 只干跑，停在 `没加 -Run，所以到此为止：不改网关、不发计费请求。`，并打印出真正会执行的 `python scripts/run_tool_eval.py --only-intent ACTION_ORDER`（补了 `-OnlyIntent` 透传才到得了这一行）。整轮 17 步门禁全绿：落点 `logs/acceptance-run-20260911-212011.log`，头两行 `commit=9eede6d 开跑时工作树=clean` / `开始 21:11:40 结束 21:20:11 总耗时 511s`，README 矩阵行与之一致；跑完复读仍是 `tokensUsedToday: 0`、`llmMode: local`、日预算 260000。上一轮同口径落点是 `logs/acceptance-run-20260911-123307.log`（`commit=3e8d4ca`、489s、`开始 12:24:57 结束 12:33:07`），它验的代码不含第二轮审查的脚本与 gold 改动；18:47 那一轮（`9353a32`、528s）又因第三轮审查改了文档与一处纯注释而让位。同一批代码在换落点之前还有两轮没全绿（`200038` 16/17、`201808` 15/17），这两轮的失败原因记在文末「第三轮收尾」。更早还有**另外**两轮同批改动没收口（`121933` 那一轮 16/17，与 C 盘只剩 19 MB 让 `build`+`unit` 两步全红的那一轮），它们与上一句的 `200038`/`201808` 不是同两次，失败原因都记在这儿，不挑一次好看的写：① `logs/acceptance-run-20260911-121933.log` 是 16/17，红的只有 `polarity` 且 exit 3 = 它自己打印的「前置不成立」（那一次 L2 里没写进源条目），脚本按 README「假红」一节既有的口径拒绝把这一格当成防线失效的证据（2026-09-10 19:29 那一轮就是这么处置的）——单独重跑 `verify-polarity.ps1` 是 exit 0 全绿，所以判它一次性；② 更早一轮 `build` + `unit` 两步直接红，`TenantIsolationAndIdempotencyTest` 6 项全 Error，栈底是 `Could not initialize inline Byte Buddy mock maker ... Could not self-attach to current VM using external process`，根因是系统盘 C 剩 19 MB（`C:\Users\Shing\AppData\Local\Temp\wsl-crashes` 里 10 个 WSL core dump 各约 1 GB），ByteBuddy 往 `java.io.tmpdir` 写 attach 探针写不下去。处置是把 `TMP`/`TEMP` 指到 `D:\tmp` 再跑，之后 6/6 绿；本轮没动任何 Java 代码，也没删那些 dump（在仓库外，等用户处置）。这条环境例外同时解释了为什么取证四件套与门禁的 `eval` 步都在同一台机器上跑：换机器时先确认 `TMP` 所在盘的余量。EOL 复查：`.py` / `.jsonl` 仍全 CRLF，Java、README、CONTEXT、ticket 16、本票仍 LF，ticket 12、PLAN、问答库仍 CRLF，无 BOM 变化；`git diff --stat` 每文件都是定向改动，无整文件重写。

### 第二轮双轴审查之后的取证复跑（口径一字未改，只是把自述钉得更严）

- 断言 36 → 40，逐桶相加等于 40（第一轮那版分桶只加到 33，是 Standards 轴自己抓出来的）。
- 新增 4 条：串号标记取值必须真在 `SeedRunner` 里、8 条越权样本带满三件套、对照组夹具钉准 `ok=16`、
  店名防呆改成双向包含之后补的两条反证（整串「生鲜超市」、「T001生鲜超市」）。
- `--selfcheck` 对照组的门槛从 `>= 7` 提到 `== 16`：钉的是文档自述那个准确数。`--selfcheck` 自己在真跑前的
  那道 `>= 7` 闸门按票面原样不动——改它会改变真跑的行为，而本轮只准改"自述有没有人守"。
- `blank_scores()` 把请求失败行的每条断言压成 `False` 而不是"未观测"，核完确认是**有意的保守方向**：
  记未观测等于把失败从分母里摘出去，反而让读数变好看，那正是铁律要防的那类事。补注释说明，不改行为。
- `--rescore` 全套重跑：差异集合仍恰好 `{ACT-ORD-09, 11, 16, 17}`，聚合仍 `168/180 = 93.3% -> 172/180 = 95.6%`，
  新产物与 `tool-eval-20260911-042142-rescore.csv` **字节级相同**（补满地址标记不改变离线判定，因为串号那一维
  在离线口径下本来就整体记未观测），所以文档里那个产物名不用换，重跑出来的临时产物直接删掉。
- 换行符基线断言写完一分钟内就抓住了我自己：一次编辑往 `run_tool_eval.py` 插进 8 个裸 LF，它当场报
  `FAIL  scripts/run_tool_eval.py=crlf实际 crlf=782 lf=8`，归一后回到 40/40。
  这条正是第一轮回的"文档声称有 EOL 断言而脚本里没有"补出来的东西，第一次独立兑现价值就抓到了本轮自己的手。

### 第三轮收尾（2026-09-11 晚，`11a12ac` → `9353a32`）

判据与口径一字未改。这轮只做两件事：把 `11a12ac`（改了 `scripts/*.py` 与 `eval/*.jsonl`）之后失效的门禁落点重新钉回机器上，
以及修掉一个把门禁冻死的起栈缺陷。执行计划与逐步状态在 `.scratch/shoppilot-mvp/round3-plan.md`（放在 `issues/` 之外，免得被 `collect_interview_questions.py` 当成第 21 个 ticket）。

- **取证四件套重跑**（18:05-18:11，跑在 `11a12ac` 的代码上）：`verify_eval_judge.py` **40/40**；`--selfcheck` 打印 `ok=16`；
  `build_eval_set.py` 退出码 0、`对抗样本 72/180 = 40.0%`、十个意图各 18 条；`--rescore` 退出码 0，差异集合仍恰好那 4 条、
  聚合仍 `168/180 = 93.3% -> 172/180 = 95.6%`、`ACTION_ORDER 72.2% -> 94.4%`、未达 95% 的行仍是 5 行；
  本轮重算出的新产物（临时文件，跑完即删、不入库）与被引用的 `tool-eval-20260911-042142-rescore.csv` **sha256 逐字节相同**（`BE53815C…5D85`）。
- **抓到一处上一轮没收口的自述**：验收第 17 条那个 `tool-cases.jsonl` 的 sha256 前缀还是旧值。这条是本轮取证跑出来的，不是审查报出来的——
  第 17 条按实然改写，改的是自述，判据没动。
- **第一次门禁跑冻死**（18:11:38 起，只到第五步）：`scripts/up.ps1` 的 `[2/6]` 在本机 Ollama 服务没在跑时，第一句 `& ollama list`
  会把 Ollama 的应用与服务进程拉起来，那几个进程继承了本步骤的重定向输出句柄，PowerShell 等不到 EOF，整条起栈流程永久冻结——
  现场是 pwsh 活着、19 线程、CPU 停在 5.546875 不再涨、**没有任何子进程**、`8082`/`8091` 始终不通、`logs/acceptance/stack.log` 停在 18:14:04，
  最后四行是被拉起的 Ollama 自己打的 `INFO`（它们的日志出现在我们的日志流里就是句柄被继承的实证）。18:36 手动终止，这一轮没产出落点日志。
  难看的地方在于 `up.ps1` 那句 `Ollama 未监听 11434，先跑 ollama serve` 的 fail-fast 一直写在 `ollama list` 之后，**从来就不可达**。
- **修法**（`e7c19ad`，只动 `[2/6]` 一段）：探活前置——端口没在听就用仓库既有的 `Start-ShoppilotService`（`Win32_Process.Create`，日志写自己的
  `logs/ollama.out`）脱离式拉起 `ollama serve`，`Wait-For` 到 11434 之后再问 CLI。两条机制验证都在同机跑过：
  ① 用同法起一个活 60 秒的 `powershell Start-Sleep 60`，2 秒后它 `alive=True`、父进程 `WmiPrvSE.exe`，而外层 pwsh（stdout 重定向到文件）3.5 秒就退出；
  ② 拿一个没人用的 `11435` 端口跑改后的代码形状（不碰别人共用的 11434），端口 5.7 秒起来、之后那句 `& ollama list` **0.17 秒返回**、脚本 7 秒收工。
  **旧写法此刻复现不出来**（Ollama 应用已在跑，触发条件消失），所以那句成因是从上面那串日志证据推出来的、不是重跑出来的——这条区别留在这儿，免得下一轮把它当实测结论引用。
  这条缺陷与本票判据无关，它砸的是 PLAN 承诺项「新机器照 README 一条命令起栈并跑通三条演示」，所以记在本票的收尾里而不是另开票。
- **第二次门禁跑**（18:47:20-18:56:08，HEAD `9353a32`、开跑时工作树 clean）17 步全绿、总耗时 528s，落点 `logs/acceptance-run-20260911-185608.log`；
  `build` 与 `unit` 两步的 surefire 模块合计都是 `3 + 12 + 89 = 104`（两份日志的 `Results:` 段逐条对过）；README 矩阵块、落点 prose、索引行同步换到这一轮。
  各步耗时与上一轮同量级但确实偏高（`stack` 89s 对 70s、`polarity` 36s 对 27s）。`stack` 那一步有现成的嫌疑人：**我自己**——18:48:17 我并发跑了那次 11435 隔离实验（`logs/ollama-probe.out` 首行时间戳），它另起一个带 GPU discovery 的 `ollama serve`，正落在 `stack` 步的窗口（18:47:20-18:48:5x）里。这句先记下不深挖，耗时不当判据；重跑那一轮门禁期间不再并发任何东西，让这一格重新变干净。
- **冒烟产物换轮**：`tool-eval-20260911-211809-local-smoke*`（落点轮，README 索引行引用的就是它）、`185410`（第 11 条那 1 格抖动的对照面）和
  `tool-eval-20260911-123109-local-smoke*`（第 11 条的跨轮对照）两份留库；`tool-eval-20260911-050015-local-smoke*` 用 `git rm` 删除——
  第 11 条改写后它不再被任何文档引用，而它提供的那一份读数已由 12:31 与 18:54 两轮的对照承担。删的是产物，不是判据；
  `tool-eval-20260911-042142-rescore.csv` 照旧在库、仍被 README 两处引用。删前删后各跑一次全库引用扫描：除本节这段"删除记录"本身，
  再没有任何文档把 `050015` 当证据引用。也就是说本节留的是**被删对象的身份**，不是对它的依赖——照登它删了，比抹掉它更好核对。
- **第三轮双轴审查（fixed point 仍是 `e64dc66`）报 9 + 2 条，逐条核实后 11 条全部成立、没有一条是误伤**，最重的一条打在我自己的取证动作上（第 11 条那句假的"零差异"，两条轴各自独立抓到）；其余：第 23 条问答数没跟着重生成改、"本轮改动全部落在…"这份清单不全、`stack` +19s 那句"没有可归因的环境变化"其实是假清高（嫌疑人就是我自己 18:48:17 并发的那次隔离实验）、README 矩阵那格暗示新的起栈分支在门禁里跑过（并没有，本机连 `logs/ollama.out` 都不存在）、索引第 19 行的 `69s / 13s` 与任何一轮落盘都不符、`lib-launch.ps1` 的 dot-source 理由注释漂了、`console` 停栈口径没写 `11434` 不会被停、被我补丁吃掉一格的续行缩进。全部闭环在 `c5e6c2e` 与 `9eede6d`。
- **换落点为拿 17/17 又跑了三轮**（`c5e6c2e` 之后代码没再动过行为）：20:00 那一轮 16/17（`polarity exit 3`）、20:18 那一轮 15/17（`polarity exit 3` + `console` 红在 `3 chunks / 60 chars` 对 `> 60` 字，差一个字），两次红的是吃本机模型推理的那两步：当时可用内存 2.1-2.5 GB、该网关进程 `shoppilot_cache_embed_unavailable_total` 累计 13 次而 `polarity_blocked` 为 0（向量没跑到守卫那一步）、`OLLAMA_MAX_LOADED_MODELS:1` 让 bge-m3 与 qwen2.5:3b 互挤（`/api/embed` 冷加载实测 5.43 s）。21:11 那一轮在可用内存回到 3.3 GB、门禁期间不并发任何东西的条件下 17/17 全绿 511s，`polarity` 守卫计数器增量 1、`console` 15/15。**这一路没放宽任何断言**：`console` 那句 `streamedChars > 60` 与 `polarity` 的 `exit 3` 语义一字未动；`verify-polarity.ps1` 在"探针自身也没拿到 L2 候选"时把前置不成立报成 FAIL 而不是 exit 3，是这轮暴露的真问题（形状与本票第 2、4 处同族），但它改的是防线脚本的判据，另开一票做，见计划文件 P9。
- **零额度**：每一轮 17 步跑完都复读一次 `GET /api/v1/support/ops/circuit`，18:57 与 21:21 两次读数都是 `tokensUsedToday: 0`、`llmMode: local`、日预算 260000（`eval` 步那 24 条走 ollama `qwen2.5:3b`，不计费）。
  本轮改动全部落在：`scripts/up.ps1`（起栈修复）、`scripts/lib-launch.ps1`（第三轮审查后补的 dot-source 理由注释，纯注释）、README、本票、新增的 `.scratch/shoppilot-mvp/round3-plan.md`、`docs/interview-qa.md`（重生成，90 -> 91 问）、`docs/console.png`（门禁 `console` 步自己重跑写的截图，不是我手改）、以及冒烟产物换轮。阈值、判据、gold、夹具、断言数一处未动。

## Handoff notes

**关键决策（收尾时补记，与文首「决策」的区别是：那 6 条是动手前拍板的，这里是实现中被事实改写的部分）**

1. 换串号标记时踩到计划里没写的一层：品类名与快递商名**不能**当标记。它们来自 `SeedRunner` 的全局
   `CATEGORIES` / `CARRIERS` 词表，任何租户谈自家生意都可能合法说出；街道名同理（`文三路` 四条种子订单共用），
   只有带门牌的整串才私密。所以计划里写的 `["果蔬","13800001234"]` 是错的（果蔬既不是 90001 的字段，
   也不能当标记），最终取 `演示买家` / `13800001234`，两条改地址样本另加 `文三路 1 号`，并把这条约束
   做成校验器的 `SHARED_VOCAB` 防呆，而不是记在脑子里。
2. 参数子指标最终按三分法报，不是"6 降到 3"：明细的 `actual_args` 只存了**旧 gold 期望那个工具**的入参
   （`link` 按旧期望工具名取），所以放开合格答案集之后换过去的工具参数离线根本取不到。三条是"待重跑"、
   三条是"天然不可比对"，性质不同，混成一个分母就会算出 `57/60 = 95.0%` 这个不存在的读数。
3. 越权补强的落地形态从"给老用例加两条断言"改成"新增一条独立用例"：`logisticsSharesTheSameOwnershipGate`
   一正两反，正向只断"不是 `NOT_FOUND`"——10001 的状态由随机种子决定，未发货时它该回
   `STATE_NOT_ALLOWED`，不能对归属者谎称查不到这单（ticket 03 修过这个谎）。用例总数因此是 103 → 104
   而不是计划里写的 ≥105，验收第 20 条按实然改写。
4. 判据唯一性那条验收原写"`rg tool_ok =` 恰好 1 处"，实现中发现它会被 `judge()` 的两个分支（期望调工具 /
   期望不调工具）自然打破，也会被 `rescore_details()` 里那句读历史值的 `old_tool_ok = ...` 误伤。
   改成结构断言：锚定 `^\s+tool_ok = ` 且全部落在 `judge()` 体内。量的是"判据只有一份"，不是"只有一行"。
5. 证据从"手工截图"升级为一份可重跑的证据跑器：`scripts/verify_eval_judge.py`，40 条断言，全部改动在
   仓库外 `tempfile` 副本上做，跑完反查工作树未被污染。变异反证只有在"改坏了必须红"成立时才算证据，
   所以对照组（干净副本必须绿）与被试组同批断言。
6. 第一轮 code-review（Standards + Spec 双轴）报出 6 条，逐条核实后全部成立并修掉，没有一条是"审查意见误伤"：
   ① 文档声称 EOL 有机器断言而脚本里没有 → 补 `EOL_BASELINE`（17 个文件）+ 断言；② `admission_ok` 空单元格静默判错，
   与 `status_ok` 同型 → 第 4 处缺陷；③ 取证脚本在默认 GBK 控制台下 `UnicodeEncodeError` 崩、且崩之前假报 2 条结构 FAIL；
   ④ 「对偶矛盾」定义写成「同订单」，与自己放开的 `ACT-ORD-16/17` 两组互斥，且「全表只有 4 组符合」说过头；
   ⑤ README 指标表格子漏写 `NOT_FOUND` 离线未观测的条数；⑥ "其余 176 条逐格一致"只对 `tool_ok` 一列成立。
   ④⑤⑥ 全是措辞与自述超出证据范围的虚高，①②③ 是"声称有反证其实没有"。一轮双轴审查把本轮自己的
   证据链收窄回它能撑住的宽度，这正是把判据写成机器断言的意义。
7. 第二轮双轴审查（修完第一轮之后再跑一次）又报 8 条，核完 6 条成立、2 条不成立，成立的全部当场闭环：
   ① README 复现段那行还留着"103 项"，而同文件矩阵段已经是 104 —— 同一文件里两个自述打脸；
   ② `accepted_tools()` 的 docstring 仍写"同租户、同订单"且说这条由 `build_eval_set.py` 的校验器把门，
      而校验器只看合格答案集的形状（合法/非空/不重复），真正逐条找对偶的是 `verify_eval_judge.py` 的
      `dual_partners()` —— 把两道门混成一道，正是第一轮①的同类；
   ③ `build_eval_set.py` 注释写"三条防呆"而实装 5 个分支；④ 文档给 36 条断言分桶，逐桶相加只有 33；
   ⑤ 串号词表对拍只钉住"禁词表不漂"，没钉住"标记的取值本身还在种子里"——`SeedRunner` 一改演示收件人，
      8 条串号断言一夜之间变成恒绿摆设，形状与本轮第 2、4 处缺陷一模一样；⑥ 同一轮还发现 8 条越权样本里
      只有 2 条带地址标记，而 `queryOrderDetail` 返回整份 `AddressView`。
   ⑤⑥ 的修法是给标记补一条"取值必须真实存在于种子"的反向对拍 + 8 条样本一律带满三件套，
   并把店名那条防呆从"标记⊆店名"改成"互为子串都拦"（整串招牌词与"招牌词+更多字"同样不配当标记）。
   断言数从 36 涨到 40（新增：标记取值对拍、三件套齐备、对照组夹具=16 准确数、店名整串、店名加长串）。
   **两条不成立的也记下来，免得下一轮又被当成真问题重做**：审查说"取证命令缺 `--selfcheck` 那一行"
   与"改动清单漏了 ADR 与 README"，实际两处都在（`--selfcheck` 在取证命令第 2 行，改动清单第 51、52 行
   就是 ADR 与 README/ticket 落点）——子代理读的是旧快照。核实成本一条几秒钟，比照单改便宜得多。
8. 第三轮收尾被一个跟判据无关的地方绊倒：门禁 20 分钟不动，卡在 `up.ps1` 的 `[2/6]` 第一句 `& ollama list`——
   模型服务没在跑时 CLI 会顺手把它的应用与服务进程拉起来，那几个进程继承了本步骤的重定向句柄，PowerShell 等不到 EOF。
   教训不是"记得先起 Ollama"，而是**别拿被测对象的 CLI 当探活**：探活要用脱离式启动 + 端口断言。顺带一条更普遍的：
   脚本里那句"先跑 ollama serve"的 fail-fast 写在会挂死的那句之后，等于没写——**报错信息也有可达性这回事**，
   它和第一轮那条"文档说有 EOL 断言而脚本里没有"是同一类毛病，只是这次红在墙钟上而不是红在读数上。
9. 收口审计（22:5x，**上一版跑器**：54 项机器断言全绿、退出码 0、全程 `tokensUsedToday` 前后均 0）净新增两处文档订正，都在计划文件里，判据面零改动：
   ① P4 原写「除 `050015` 那一处外全部命中在库文件」——这句当时就不成立，同一批文档里还有第二条 MISSING，
      是 P2 自己引的复算产物 `tool-eval-20260911-181037-rescore`（它按流程跑完即删）。现在两条一起钉成"被删对象身份记录"的具名豁免，
      并加一条反向断言：豁免名一旦不再出现在文档里就红，防止豁免表长成永久免检通道。
   ② P3 那三行 `185608 / 528s / stack 89s` 是本项第一次换轮的读数，落点随后又换到 `212011 / 511s / stack 80s`；
      历史数字不改写，只补一句限定，免得下一位读者拿 `89s` 去对 README 的 `80s`。
   审计自己也栽在比对原语上第二次：第一版用 `Select-String` 扫引用，默认大小写不敏感，把 `PLAN.md` 匹配到 `round3-plan.md` 报假 FAIL；
   换成 `os.listdir` 精确集合后归零，并把"`plan.md` 必须判不存在、`PLAN.md` 必须判存在"做成常驻对照组。
   与本项第 11 条的 `Compare-Object` 是同一课：**取证动作本身也需要反证**，否则它只是另一把没校过的尺子。
   收口脚本按第四轮审查的意见**入仓**：`.scratch/shoppilot-mvp/round3-closeout-audit.py`（放 `.scratch` 不放 `scripts/`，否则按 P7 口径改了 `scripts/` 就得重跑 17 步门禁，而这台机器今晚只剩 1.1 GB 可用内存），读数产物同目录 `round3-closeout-audit.txt`。它钉的是当轮常数（落点轮、耗时、sha 前缀、104 用例数），换落点就得同步改它——这与 `verify_eval_judge.py` 里 `EOL_BASELINE` 同一类「有意钉死的基线」。可复现物是本票文末「取证命令」那 4 条 python 命令 + 1 条零额度复读 + 这一支审计。

10. 第四轮双轴审查（09-12 00:1x，fixed point `9eede6d`；Standards 轴 6 条 + Spec 轴 5 条）。**账按两份报告原文与磁盘重核，不抄上一轮的收口笔记**：
    11 条全部成立，10 条订正完、1 条（下面第 ⑤ 项）只订正一半。落地面是 **19 处跑器替换**：
    19 处由两支一次性跑器做（pass1 16：README 4 / 本票 5 / 计划 7；pass2 3：本票 2 / 计划 1；每处 `count==1` 断言，任一命中数不是 1 就整批不提交），
    另有手工结构性改动（例：计划开头那行计数口径要同时删掉一句不可复核的自述，不适合放进按子串替换的跑器里）；
    这类改动**不计入上面的处数、也不另行报数字**——它没有能重算的产物，写出来就是下一个不可复核自述（见本条 ⑫(d)）。
    ⑪ **本轮最该记的一条是自己**：为了验"这两支跑器幂等吗"，我把没带守卫的 pass1/pass2 各重跑了一遍，
       于是**它们真的又应用了一次**——有几处的 `new` 里原样含着 `old`（为了让替换后的句子读起来连贯），
       所以"old 还在文中"根本不是"没应用过"的判据。结果 README 一段与计划两段被贴成重复内容（该段 454 字是**当时读数**：未提交中间态的控制台输出，盘上无产物可重算，不当实测结论引用）。
       处置：`round3-doc-fix-repair-dupes.py` 按"**两份逐字相同且紧邻**"这一条形状判据摘掉多余的那份
       （不重打原文——我第一次就是重打锚点抄错字，脚本按"锚点不在第一份里"直接拒绝动手，那次拒绝是对的），
       再给两支跑器补**三态幂等守卫**（`new` 已在文中 = 已应用跳过；`old` 恰一次且 `new` 不在 = 应用；其余 = 漂移报 FAIL 停下来查），
       并用 sha256 复验：重跑后 README / 计划 / 本票三份文档**逐字节不变**（`共 16 处：新应用 0、已应用跳过 16、失败 0`、
       `共 3 处：新应用 0、已应用跳过 3、失败 0`）。
       这是本票第 11 条那课（取证动作本身也需要反证）第四次现形：前三次是 `Select-String` 大小写、`Compare-Object` 吞差异、
       手搓正则漏行，这次是**拿"重跑一遍"当验证而不先证明重跑是安全的**。
    ① **Standards F1（硬违规）**：README:586 那句「`69s / 13s` 那个旧读数与本机任何一份落盘矩阵都不符」是**假的全称否定**，
       而且是我在第三轮订正别人虚高时自己写下的——方向和铁律相反（把话说的比证据强）。审计 H2 用逐轮矩阵解析钉住
       `logs/acceptance-run5.log` 里 `stack 0 ok / 69s` + `demo 0 ok / 13s` 确实成对存在（09-09 的真读数，只是不属于本轮任何一次跑法），
       H2b 钉住 README 里那句话只许以「我说过头了」的撤回形式出现（对照组：订正前那份 README 必须判红，否则 H2b 自己是恒绿假防线）。
    ② **Standards F2（硬违规）**：计划 P4 以「README 索引行 + 第 11 条」为理由留 `185410-local-smoke*`，但 `d8beb32` 换轮后 README 索引行
       已改引 `211809`，`rg 185410 README.md` 零命中——`:108` 改了、`:109` 没改，同一笔提交内部自相矛盾。留库理由改成「第 11 条那 1 格抖动的对照面」，
       并加 H4b 钉住那个主张句式不许回来（同样带订正前对照组）。
    ③ **Standards F3 + Spec 第 4 条（硬违规·口径）**：README 断言 `200038`/`201808` 两轮「成因是机器不是防线」，却没做本计划 P2
       明文要求的「栈不动单跑 `verify-polarity.ps1`、exit 0 才算一次性」；而替代证据（可用内存 GB 数、`embed_unavailable_total` 13、
       冷加载 5.43 s、`3 chunks / 60 chars`）当时是命令行手抄，`logs/` 不入库、逐步日志又被 21:11 那轮覆盖，**现在复原不出来**。
       订正：README 把这两类话收回「这是推断、不是实测结论」，缺的那次复验连同 `verify-polarity.ps1` 的前置判定一起并入另开的那张票；
       并在 H5/H5b 把取证面口径写死——`logs/` 整目录与 `*.log` 在 `.gitignore` 第 9-10 行里，**干净克隆里没有落点日志**，
       入仓的机器证据只有 `eval/results/` 的 CSV/meta、`docs/console.png` 与本票文末这一支审计（脚本 + 读数产物）。
    ④ **Standards F4（judgement）**：README 立了规矩「记的始终是『脏的是什么』或『为什么干净』」，但 `201808` 那一轮头两行实为
       `开跑时工作树=dirty（4 项）` 没照登——正是我自己定的规矩要求的那一格。已补登那 4 项是 `docs/console.png` 加 `195822` 那一轮冒烟的三份文件
       （都是前一轮门禁自己写出来的生成物）。
    ⑤ **Standards F5（judgement）**：本票第 24 条连着两句「…还有两轮没全绿」/「…在此之前跑过两轮都没收口」，看起来像同义反复。
       **对盘重核后有反转**：这两句指的是**不同的两轮**（前一句是 `200038`/`201808`，后一句带 ①② 展开的是更早的 `121933` 与 C 盘满那轮），
       真缺陷不是内容重复而是**措辞撞车**——两个"两轮"、两句一模一样的"不挑一次好看的写"紧邻在一起，读者（含审查轴）会误读成同义反复。
       所以做的是**消歧不是删除**：前一句补上逐轮形状读数并把尾句那份重复收掉
       （→「（`200038` 16/17、`201808` 15/17），这两轮的失败原因记在文末「第三轮收尾」。」）；
       后一句起手点名它说的是哪两次、并显式声明与前一句不是同两次
       （→「更早还有**另外**两轮同批改动没收口（`121933` 那一轮 16/17，与 C 盘只剩 19 MB 让 `build`+`unit` 两步全红的那一轮），
       它们与上一句的 `200038`/`201808` 不是同两次，…」）。①② 的展开原文一字未动。
       **跨文档那三处不合并**，理由是本仓库的口径是「每处读者要能自己核对」：
       README 是对外结论、本票是取证账、计划是记账，合并成单一出处会让另外两处失去自足性；代价照登——这三处读数今后要一起改。
    ⑥ **Standards F6**：Status 行写「另外三次没当落点」，收尾段实列四次（18:11 冻在起栈、`200038`、`201808`、`185608` 让位）——改成按形状数清楚的四分类。
    ⑦ **Spec 第 1 条**：P8 一边写「54/54 全 PASS」一边写「故不入仓」，等于让本轮标题性主张没有产物可核；且 P4/P8 两条**限定语当时写在「通过判据」正下方**，
       读起来像事后放宽判据。处置：审计脚本与读数产物一起入仓（上面第 9 条末行就是这次改写），两处限定语**挪回证据侧、判据本体一字未动**。
    ⑧ **Spec 第 2 条**：「取证命令 5 条」实为 4 条 `python` 调用（`--rescore` 那条是续行）；「共 27 个具体引用，MISSING 0」按不同去重口径复算得 25/46——
       同一批名字换一种数法就给不同数。两处都改成「只引用审计自己打印的机器读数，不再手写数字」。
    ⑨ **Spec 第 3 条**：本票第 12 条把行号写成 `run_tool_eval.py` 的 152 与 156（这里故意不写成「冒号前缀」那种机器可解析的形状：它们是**已废弃的引用**，写成那个形状会被审计 H3 当成一处活引用要求核对，而盘上那两行的内容早就不是了），`rg -n --case-sensitive "^  tool_ok = "` 实打在 156、160
       （都在 `judge()` 体内，`def judge` 起于 144 行——实质结论不变，是引用行号过期）；第 13 条写 `rescore_details()`（94 行）也错，实为起于 421 行、函数体 92 行。
       这一类现在钉成审计 H3：文档里每一处 `file.py:NNN` / `file.ps1:NNN` 都要能在对齐的磁盘行上落住，钉不住即红。
    ⑩ **Spec 第 5 条**：P9 标题还写「待用户处置」而状态已是「Pass（自发解决）」；计划开头「共 9 步（P0-P8）、合计 26 条可验项」没随 P9/P10 更新。
       标题改成中性描述，计数口径改成只自述机器能重算的两件事（步数由 H6 钉、断言项数由 `round3-closeout-audit.txt` 末行钉），
     那句「17 条可逐条打勾的动作项」删掉——整个计划文件没有一个复选框，那个数只能靠手搓，属不可复核自述（审计 H6c 钉住它不许回来）。
  ⑫ **收口时又抓到自家量具与自述五处假绿，全在审计脚本与文档自身，不是审查轴报的**：
     (a) **H9「长段落自我复制」检测器是恒绿假防线**。它用 200 字窗、`step=50` 采样、`range(0, len-win)` 上界，两处缺陷叠加：
         一是上界少算末位（恰好两份、总长 400 字的探针，第二份起点 `i=200` 落在界外，永远采不到）；
         二是更要命的那条——**只在 50 的整数倍上采样，等于只认得与网格对齐的复制**。本次真实事故被贴两遍那段在 README 里的起点
         偏移是 `33880 % 50 = 30`（33880 为该段在 README 里的起始下标，**当时读数**、盘上无产物），正落在网格外，两份逐字相同的窗错位半格、永远配不上对。所以 H9 当时判绿是**瞎**，不是**干净**：
         它抓不出的恰恰是它自己声称要防的那一类事故。改成 `step=1` 逐字扫描 + 闭区间上界后，三份文档仍 0 命中（正向不变），
         而两个对照组（H9b 把 README 真实长行原样贴两遍、H9c 把第二份起点刻意顶离 50 字网格）从判不出变判得出。
         这条是本票反复在治的病的最纯形态：**一条从未判过红的防线，等于没有防线**；区别只在于这回发病的是我为「防自述造假」写的那支脚本本身。
     (b) **审计自己的项数少算**。自我指涉的 H7/H7b 夹在 H6 与 H8 中间求 `N`，于是 N 天然漏掉它之后才跑的那几项，
         末行实跑项数比自述多 4。修法是把 H7/H7b 挪到全文件最后，并加硬断言 `N == len(PASSES) + len(FAILS)`：
         将来再往它后面追加 check 而忘了搬，就是当场崩，而不是静默少报。
         **本项故意不写绝对项数**：它随本轮新增的断言一格格变化，写死在这里就会成为下一个自己制造的假自述；
         唯一的现在数字写在下面「本轮读数」那一段，并由 H7/H7b 与那条硬断言三面钉住。
     (c) **修 (a) 的过程中，我新写的注释又成了另一条不可复核自述**。第一版改完，H9b 打出「贴两遍后命中 1595 处」（**当时读数**）——
         那不是 1595 次事故：一段 1794 字的块贴两遍，本来就会产生 1595 个逐字相同的窗（1794−200+1）。而我给那版函数写的注释是
         「把命中下标按连续段归并，免得同一次复制刷出一串计数」，机器读数证明那句话没落地：归并键取的是**窗口的首次出现下标**，
         一次复制产生 1595 个不同的键、每组恰好 1 个元素，归并对循环从未启动。改成按**第二份的起点**归并后读数才是 1 处，与事故形状一致。
         病根与前两条同一个：**注释先于机器做了断言**，而发病的位置恰好是那一格自称「防重跑贴两遍」的防线。
         另记一条排查教训：我为了验证归并先用 `"a"*1794` 造夹具，结果该串每 26 字一循环、窗窗相同，「正常文本」对照组直接被它判出重复
         ——**同质串测不出复制检测器**，换成按位置取字的异质串后这组判据才成立。
     (d) **「本批 17 处定向替换」这个数在三处跳出来，而且是错的**。本条开头、下方「不重跑依据」段、计划 P10 第一条都写 17（= 16 跑器 + 1 手工），
         但两支跑器**自己末行打印的**是 pass1『共 16 处』+ pass2『共 3 处』= **19 处**，pass2 那 3 处（F5 消歧 2 / 计划 P7 追账 1）
         从没被任何一格的账收进去。这不是跨文档口径分岔：第 10 条自己下面就原文引用过这两串末行读数，所以它是**同一条款内部自相矛盾**。
         交接笔记里「已按实数写准（曾两次写错成 17/16，都已对盘改准）」这句在盘上找不到对应 —— **第五次**发现「上一轮记的账对不上盘」，
         而这次的账是我自己不久前才记的。处置：三处改成机器可重算的 19，并新增审计 H10 钉住它；H10 不重新解析跑器源码（那等于再造第二把尺子），
         只把两支跑器末行的『共 N 处』取出来汇总、与文档自述对拍。**手工改动的处数从此不再写成数字**：它没有产物能重算。
     (e) **往本票第 10 条追加新块时撞了圆号**：顺手写成 `⑪`，而 `⑪` 早被上面「本轮最该记的一条是自己」占了，同一条款里出现两个同号，
         交叉引用（「见第 ⑪(b) 项」这类句子）的指向随之变得不确定。新增审计 H11/H11b：
         同一个顶层条目内的圆号子项不得重复，且正文里每一处「第 X 项」引用必须落到一个真存在的条目上。
         （H11 的第一版没有「行首圆号后必须跟 `**`」这个限定，曾把三处换行落到行头的正常行文误判成重复——
         这一次是**量具偏红**而不是假绿，同样照登。）
     五处的共同教训：**对照组必须能失败**，而注释与文档里那句「我已经做了 X」，只有在机器打出 X 的读数时才算做了。
     H9b/H9c 是这一轮才真正开始兑现「自带对照组」这句文件头承诺的；在此之前那支探针（切 README 固定 200 字再贴两遍）
     落在采样网格上，是个只会跟着检测器一起瞎的摆设。
    **上一轮记的两条"驳回"本身是幻影，照登以免又被当成真问题重做**：上一轮的收口笔记里写着两条驳回（「审查说本票写 `verify_eval_judge` 17 项断言」
    与「Kant F5 报的 `up.ps1` 第 115 行 不存在」）。对盘重核：两份报告原文里都没有这两句——`rg "17 项断言"` 在本票零命中（盘上只有
    「`EOL_BASELINE`（17 个文件）」，与 40 条断言不矛盾）；`scripts/up.ps1` 有 139 行，第 115 行是入库日志过滤器那一句 `Where-Object`，
    而 Kant 真正的 F5 是上面第 ⑤ 项的重复叙事。**收口笔记自己也会造假账**（本票第三次发现"上一轮记的账对不上盘"），
    所以从这里起立规矩：复审的账只按报告原文与磁盘读数记，笔记只当索引，不当事实。
    **本日内第三次栽在比对原语上，这次是我自己**：核 `121933` 那一轮时我手搓正则数 `ok` 步、漏掉一行，把 16/17 数成 15/17，
    于是去"订正"了一格本来就对的文档（README 那格写的 16/17 是对的）。错的是量具不是文档，这条反证已固化成审计 H1/H1b：
    六轮门禁的 ok 步数逐轮机器解析、且每轮都必须真是 17 步（防"步数变少造成的更绿"）。
    **不重跑 17 步门禁的依据**（按 P7 口径核过，不是省事）：本批 19 处跑器替换与手工改动只动 `README.md`、本票、计划三份文档，加 `.scratch/` 下那支审计脚本；
    `scripts/`、`eval/`、`src/`、`docs/adr/`、阈值常量、gold 零改动（审计 B1-B6 与 B3 的 diff 面白名单核验钉住，F4 钉 README 相对链接不破）；
    新脚本落在 `.scratch/` 下，不触发「改了 `scripts/` 就得重跑门禁」那一条。另：这台机器今晚可用内存约 1.1 GB，门禁要 ≥3 GB，硬跑只会再产一轮假红。
    **本轮读数**：收口审计本轮实跑 **95 项机器断言**（项数以 `.scratch/shoppilot-mvp/round3-closeout-audit.txt` 末行为准），
    审计期间 `tokensUsedToday` 前后均 **0**（`llmMode=local`、日预算 260000），全程零模型额度。
    写法口径由 H7/H7b/H12 三面钉：「95 项机器断言」是本轮实数、必须被文档至少引用一次，且入仓产物末行必须等于它；
    上面第 9 条那个 54 是**上一版跑器**的历史数，
    本条上面那几版的 70 / 74 / 75 / 77 / 87 均为**同一支跑器在历轮内递进时的历史数**（70 是 N 算早了，见第 ⑫(b) 项；74 与 75 是后来又加了 H10（钉替换处数）与 H11/H11b（钉圆号子项不重复、引用有落点）；77 是第四轮收口那一版的实数，第五轮双轴审查后加 A3b/B7/E4b/F2c/H1d/H3b/H12/H13/H13b/J0 十项让位给 87；87 又是把 F1 按可核面拆成 F1/F1c 之前的历史数；88 是第五轮闭环落盘那一版的实数，第六轮闭环加 J0b/H8b/H8c/H14/H15/H16 六项后让位给 94；94 是 P12 落盘那一版的实数，第七轮闭环加 H12b 一项后让位给 95）；
    凡不等于本轮实数的项数，所在行必须带历史标记，不允许裸着当现状。

11. 第五轮双轴审查（09-12 上午，fixed point `a6ccdcb`；Standards 轴 9 条 = 6 硬违规 + 3 judgement、Spec 轴 6 条）。
    **账按两份报告原文与磁盘重核**：合计报 15 条，两轴有 2 条同指一处（Spec(b)＝Standards 第 6 条、Spec(c) 的 H3 期望串＝Standards 第 7 条），
    去重后**独立成立 13 条、驳回 0 条**。上一版收口笔记把这笔账记成「Standards 6 + Spec 5、成立 11、驳回 3」，
    又把「成立 11」拆成「9 硬 + 2 judgement」——三处都对不上报告原文（6+5=11 是**第四轮**那对轴的数，Standards 自己写的是 6 硬 + 3 judgement）；
    **第六、七次**发现上一轮记的账对不上盘，且这次的账仍是上一轮的自己记的（幻影驳回那两条见第 10 条末尾）。
    计划 P10 来源句已按「报 N = 成立 M + 驳回 K」算平，P11 来源句按两份报告原文重列，处置清单从 ①-⑪ 扩到 ①-⑬。
    落地面全在量具脚本与文档自述上：**审计新增十一项断言** A3b / B7 / E4b / F1c / F2c / H1d / H3b / H12 / H13 / H13b / J0，
    文档自述按 11 条逐条订正（计划 P11 的 ①-⑪ 与本条一一对应）。手工替换的处数**不写成数字**——它没有能重算的产物，
    按第 10 条 ⑫(d) 立的规矩，写出来就是下一个不可复核自述。
    ① **Spec 第 1 条（硬违规）**：审计在干净克隆上跑不出来。G 组 7 项 + H1/H1b/H2 共 10 项读不入库的 `logs/`，
       A1 钉 `origin/main==HEAD`、A3 打活体网关，缺前置时直接抛 `FileNotFoundError`，而脚本头行自述「重跑…退出码 0 = 全绿」。
       处置：头行改成如实自述（本机当轮对账单，不是干净克隆可复现的防线）+ 缺前置走**具名 SKIP** 并以退出码 3 收口
       （与 `verify-polarity.ps1` 的 exit 3 同族），新增 J0 钉「空目录喂解析函数必须返回『不适用』而不是崩」。
    ② **Spec 第 2 条（硬违规）**：三处文档把「项数以 `round3-closeout-audit.txt` 末行为准」钉成权威，可没有任何断言读这份产物。
       处置：新增 H12 读 `git show HEAD:` 那一份的末行 `共 N 项` 与本次实跑对拍（读 HEAD 不读工作树——工作树这份正被本次 Tee 边跑边写，读它是自我循环）。
    ③ **Standards F2（硬违规）**：F2 对照组恒绿——探针名物理上不可能存在，且把 F1 的谓词就地重写一遍（同一段复制在 331/333/343 三处）。
       处置：抽出单一谓词 `resolvable(stem)`，F1/F2 共用；F2 真调它，并加 F2c 正向对照（真实产物名 `tool-eval-20260911-042142-rescore` 必须判可解析），
       防 F2 单边恒绿。
    ④ **Standards F3（硬违规）**：E4 恒绿——只判「合并」「否决」两个词在不在 README 里（实测各 13 与 8 次），名字却写「仍写明合并工具这条路被否决」。
       处置：改成钉同一行同时含「合并」「否决」「queryOrderDetail」三个特征（实测命中 README:280），并加 E4b 反证：内存里删掉那一行后判据必须变 False。
    ⑤ **Standards F4（硬违规）**：H1 是第二把尺子——注释称「README 里每一句 N/17 必须等于日志实算」，代码从不读 README，只比脚本里手抄的字典。
       处置：H1 降级为「与钉住的期望表逐轮相符」（只管日志这一侧），新增 H1d 真去 README 抽主张：同一行里既有 `acceptance-run-<ts>` 又有 `N/17`
       才算一处，逐处与日志实算对拍。实测抽到 3 处（`200038`→16、`201808`→15、`121933`→16），README 别处的 `ACT-ORD-09/16/17` 不误命中。
    ⑥ **Standards F5（硬违规）**：P10 判据第 3 条称 `scripts/`、`eval/` 零改动「由审计 B1-B6 与 F4 钉」，而 B6 白名单明列 `scripts/up.ps1`、
       `scripts/lib-launch.ps1`、`eval/results/`，F4 只是 README 断链检查，且 B 组窗口是第三轮起点 `11a12ac..HEAD`，不是本轮 fixed point。
       处置：新增 B7——窗口取本轮 fixed point、白名单收成**具名文件**（第六轮抓到原先两处错：窗口误用第四轮的 `a6ccdcb`，白名单写成 `.scratch/shoppilot-mvp/` 整目录前缀＝本目录下的免检通道），`scripts/`、`eval/`、`src/`、`docs/adr/` 出现即红；
       P10 那句引用改指 B7（改的是括号内的引用，判据主干一字未动；H14 钉主干与 `9d444c9` 逐字相同、并照登括号内 2 处改动，见本条第 12 条 ①）。
    ⑦ **Standards F6（硬违规）**：H3 对 `up.ps1:58` 钉的期望串是 `"# "`——任何注释行都满足，等于没钉；而文档那句 fail-fast 引用按订正前那份文件
       成立（`git show e7c19ad^:scripts/up.ps1` 第 58 行确是 `throw 'Ollama 未监听 11434…'`），订正后同一句落在第 68 行。
       处置：H3 期望串换成该行真内容（有判别力）、文档集扩到与 F1 同 8 份、`rglob` 多命中即红、detail 打印核了几处；文档引用改成「订正前那份第 58 行（现第 68 行）」；
       新增 H3b 对照组：超行引用必须被 `cite_ok()` 判失败。
    ⑧ **Spec 第 3 条**：P10 判据第 5 条要求「三条驳回项各留机器反证」，实际只有 H3 那一格有产物。处置：判据主干一字不动（括号内的改动由 H14 照登），
       补 H13 把「`verify_eval_judge` 的 17 项断言在盘上不存在」钉成可重跑断言（跑器源码命中必须 False、文档里只许带幻影/不存在/零命中标记出现），
       补 H13b 钉「被驳回的那处 `up.ps1` 第 115 行引用在 8 份文档里确实零命中」（第六轮抓到原先那版另抄 `.count()`、只扫 4 份）；第三条（`logs/` 无痕）由 G 组与 H1 承担，在本条点名指认，不另造尺子。
    ⑨ **Spec 第 4 条**：计划 P10 里那句「这条行为本身照登在本节末」在节末没有对应文字，是新的不可复核自述。处置：删掉这个指向。
       **本条订正过程中自家量具又判红一次**：把被驳回的错引用按 `文件名:行号` 的形状抄进计划，H3 与 H13b 当场各判一条红——
       记录别人的错引用本身就会新增一处引用。改用不带冒号的行文形状后两格转绿，这条现场反证照登。
    ⑩ **judgement**：死码与未用参数 `porcelain` / `assert_line` / `tickets` / `statuses` / `_n_long` / `run_py` 的 `want_exit` / pass1 的常量 `A`
       （AST 扫 load=0）。处置：删。
    ⑪ **judgement**：本条第 10 条 ⑫ 的 `454 / 33880 / 1595` 是未提交中间态的控制台读数，当前产物打的是「取段 1794 字…命中 1 处」，盘上无产物可重算。
       处置：三处按自家口径标「当时读数」，不当实测结论引用。
    ⑫ **Spec(a) 第 2 条**：P10 那句「成立 11 条、驳回 3 条」自己算不拢（6+5=11 而 11+3=14>11）。处置：P10 来源句算平成
       「报 11 = 成立 11 + 驳回 0」，并在来源侧消歧「判据第 5 条的『三条驳回项』是准备反证的三条可疑主张、不是被驳回的三条 finding」。
    ⑬ **Spec(c) 第 3 条**：读数产物里 A1 记的 HEAD 永远比当前 HEAD 慢一笔。处置：新增 H15 钉「产物记的 HEAD 必须是当前 HEAD 的祖先或本身」。
       这条与 ⑫ 当时都漏在 ①-⑪ 之外，是第六轮双轴审查按报告原文点回来才补进账的。
    **不重跑 17 步门禁的依据**（按 P7 口径核过）：本轮改动面只含 `README.md`、本票、计划与 `.scratch/shoppilot-mvp/` 下的审计脚本和读数产物，
    `scripts/`、`eval/`、`src/`、`docs/adr/`、阈值常量、gold 零改动，且这条不再借 B1-B6/F4 的名、由新增 B7 以本轮窗口钉；
    全程零模型额度（A3 读活体网关 `tokensUsedToday=0`，另配 A3b 作离线反证：本轮窗口提交信息里不得出现花钱跑测的痕迹）。
    **反证读数（判据第 2 条）**：驱动脚本 `D:/tmp/ce4.py`，每格都在**仓库外的临时克隆**里做（正本一字未改），
    做法是「克隆 → 同步本轮工作树 → 先跑一次拿基线 → 只改坏一个对象 → 再跑一次」，逐条读数：
    ① 基线（什么都没改坏）H12 = PASS，退出码 1，红项只有 `A1 / D3 / G1 / G2` 四项，且四项都能归因于克隆本身
      （A1：克隆里多了一个同步提交；D3：见下面第 3 条的 autocrlf；G1/G2：P11 当时仍是 Pending）；
    ② 九条改坏各判红一次：入仓产物末行 `共 88 项`→`共 1 项` ⇒ **H12 FAIL**；README 那格 `16/17`→`17/17` ⇒ **H1d FAIL**；
      删掉 README 里「合并…否决…queryOrderDetail」同现行 ⇒ **E4 FAIL**；把 H3 的期望串换成盘上不可能出现的串 ⇒ **H3 FAIL**；
      窗口内新增 `scripts/zz-ce.txt` ⇒ **B7 FAIL**；同一笔提交信息写 `run-dev-guardcheck` ⇒ **A3b FAIL**；
      把「`verify_eval_judge` 的 17 项断言」裸写进计划 ⇒ **H13 FAIL**；同一行里带上那个不存在的 `up.ps1` 第 115 行引用 ⇒ **H13b FAIL**；
      把计划里一个 logs 类产物名改成不存在的轮次 ⇒ **F1c FAIL**。九条全数判红，无一条恒绿。
    3. **顺手抓到一条新的环境依赖**：`scripts/verify_eval_judge.py` 的「换行符基线：17 个文件各自守住 CRLF/LF」在
       **任何干净克隆里必红**——本机 `file:E:/Git/etc/gitconfig` 里 `core.autocrlf=true`，克隆检出时把入库的 LF 改写成 CRLF，
       而该断言钉的是 LF（`git ls-files --eol` 在正本里 `i/lf w/lf`、在克隆里 `i/lf w/crlf`）。
       这条不在本轮处置面内（要动 `scripts/` 就得重跑 17 步门禁），照登为**已知环境依赖**：
       所以「干净克隆跑审计」的形状只能是「不崩 + 具名 SKIP 清单 + 少数可归因的红」，不是全绿；判据第 3 条按这个形状验收。
    **干净克隆读数（判据第 3 条）**：`git clone` 到 `D:/tmp/sp-cc3` 后只放本轮工作树的脚本与两份文档、**不拷 `logs/`**，
    审计不再抛 `FileNotFoundError`，而是打出 **13 项具名 SKIP**（F1c、G2b、G3、G4、G5、G5b、G6、G7、G8、H1、H1b、H1d、H2），
    末行 `汇总：PASS 70  FAIL 5  SKIP 13  共 88 项`，退出码 1；那 5 项红逐条可归因（A2 是我往克隆里拷了未提交文件、
    D3 是上面那条 autocrlf、G1/G2 是 P11 当时未 Pass、H12 是克隆里入仓产物仍是上一版末行）。
    对照：改之前那份（`9d444c9` 里的脚本）在同一形状下直接崩在 `logs/acceptance/polarity.log` 的 `FileNotFoundError` 上，
    连 G 组都跑不到——这就是第 ① 条 finding 的原始形状。
    **项数为什么是 88 而不是 87**：F1 拆成 F1（`tool-eval-*`，入库可核）与 F1c（`logs` 类，本机限定）两把尺子，
    这是收口阶段按同一条理由自己加的第 11 项，不是审查轴报的；它让「缺 logs 的克隆」从整条假红变成一项具名 SKIP。

12. 第六轮双轴审查（09-12 上午，fixed point `e1c7ca5`；Standards 轴 10 条 = 7 硬违规 + 3 judgement、Spec 轴 7 条）。
    **账按两份报告原文与磁盘重核**：合计报 17 条，两轴有 2 条同指一处（A3b 的提交计数、B7 的窗口），
    去重后**独立成立 15 条、驳回 0 条**，逐条处置记在计划 P12 的 ①-⑮。落地面全在量具脚本、`.scratch/` 下两支文档跑器与文档自述上：
    **审计新增六项断言** J0b / H8b / H8c / H14 / H15 / H16，项数由 88 换成 94（88 是上一版的实数，见第 10 条末的历史数那一串；94 到 P13 又让位给 95）。
    另有两件是报告之外本轮自己抓到的：J0 块站在 `find_pair` 定义之前（实跑当场 `NameError`，上一轮只做了 `ast.parse` 没做名字绑定），
    以及两支一次性文档跑器在 `core.autocrlf=true` 的克隆上把「已应用」误判成「漂移」——**改前它在克隆里真的改写了一份文档**
    （`共 16 处：新应用 1、已应用跳过 11、失败 4`），改后 `新应用 0、失败 0` 且字节不变。
    **反证读数（P12 判据 2）**：三支驱动脚本都在**仓库外的临时克隆**里做，正本一字不改。
    ① `D:/tmp/ce4.py`（第五轮那九条，本轮复跑仍全红）：产物末行的数改 1 ⇒ H12 红；README 那格 `16/17`→`17/17` ⇒ H1d 红；
       删掉「合并…否决…queryOrderDetail」同现行 ⇒ E4 红；H3 期望串换成盘上不可能出现的串 ⇒ H3 红；窗口内新增 `scripts/zz-ce.txt` ⇒ B7 红；
       提交信息写花钱跑测的痕迹 ⇒ A3b 红；把「`verify_eval_judge` 的 17 项断言」这句盘上不存在的话裸写进计划 ⇒ H13 红；
       同一行带上那个不存在的 `up.ps1` 第 115 行引用 ⇒ H13b 红；计划里一个 logs 类产物名改成不存在的轮次 ⇒ F1c 红。
    ② `D:/tmp/ce6.py`（本轮八条，形状是「克隆 → 同步工作树 → 先跑一次拿基线 → 只改坏一个对象 → 再跑一次」）：
       `merge_denied` 改恒真 ⇒ **E4b 红**；`resolvable` 改永不命中 ⇒ **F2c 红**；`cite_ok` 改恒落得住 ⇒ **H3b 红**；
       `find_pair` 缺目录改回抛异常 ⇒ **J0 红**；`local_missing` 改永不报缺 ⇒ **J0b 红**；
       改一条 P10 判据主干 ⇒ **H14 红**；产物里的 HEAD 换成无关 sha ⇒ **H15 红**；`LOCAL_ONLY` 里删掉一项 ⇒ **H16 红**。八条全数判红。
    ③ `D:/tmp/ce7.py` 与 `D:/tmp/ce7b.py`（跑器换行符那两条）：拆掉读入规范化 ⇒ **H8b 红**（末行「新应用 1、失败 4」）；
       拆掉写回还原 ⇒ **H8c 红**（README 610 个换行里 0 个是 CRLF）。CE-b 拆的那一刀 H8b 抓不到，因为全 skip 时写回路径根本不执行——
       这条覆盖洞就是 H8c 存在的理由，照登。
       基线格（什么都没改坏）里这十项全绿；同一次基线另有 A1 / H7 / H7b / H12 红，那是「克隆里多一个同步提交 + 本轮文档
       与产物当时还没订正」的中间态，不是反证失败，照登在此免得下一句被读成「基线全绿」。
    **不重跑 17 步门禁的依据**（按 P7 口径核过）：本轮改动面只含 `README.md`、本票、本计划与 `.scratch/shoppilot-mvp/` 下
    7 个具名文件（两支文档跑器 + 审计脚本 + 读数产物 + 三份文档），`scripts/`、`eval/`、`src/`、`docs/adr/`、阈值常量、gold 零改动，
    由 B7 以本轮窗口 `9d444c9..HEAD` 钉；全程零模型额度（A3 读活体网关 `tokensUsedToday=0`，A3b 作离线反证）。
    **干净克隆读数（P12 判据 3）**：`git clone` 到临时目录、**不拷 `logs/`、不提交**，逐笔复测（每一笔都是当时那一份脚本与文档）：
    ① `b54f665`（量具修复那一笔）：不抛异常，在任何 check 之前打出 14 项具名清单，
       `汇总：PASS 77  FAIL 3  SKIP 14  共 94 项`、退出码 1；3 条红是 H7 / H7b / H12，全是「本票与计划当时还写着 88、
       入仓产物末行还是上一版」，属文档未订正，不是克隆缺前置。
    ② `9a9e17d`（文档订正那一笔）：`PASS 77  FAIL 3  SKIP 14  共 94`、退出码 1；红项换成 G1 / G2 / H12——
       G1 / G2 是 P12 当时还挂 Pending，H12 是产物仍是上一版。
    ③ 本笔（H12 改走三态、清单扩到 15 项）：`PASS 77  FAIL 2  SKIP 15  共 94`、退出码 1；
       剩下两条红就是 G1 / G2 这一对自我指涉（P12 未 Pass），H12 在克隆上判 SKIP 并点名「与入仓产物那份全绿末行不同源」。
    ④ P12 转 Pass 之后同一形状的复测：`汇总：PASS 79  FAIL 0  SKIP 15  共 94 项`、**退出码 3**，
       实跑 SKIP 恰为文件头声明的那 15 项（H16 绿）。判据 3 到这一格才算真做到，P11 那一轮只做到「不崩 + 具名清单」。

13. 第七轮双轴审查（09-12 上午，fixed point `3157cd7`；Standards 轴 5 条 = 2 硬违规 + 3 judgement、Spec 轴 4 条）。
    **账按两份报告原文与磁盘重核**：合计报 9 条，两轴有 2 条同指一处（改动面自述 6 vs 7、清单 14 项 vs 15 项），
    去重后**独立成立 7 条、驳回 0 条**，逐条处置记在计划 P13 的 ①-⑦。这一轮抓的全是自家量具：
    **H14 只比判据首行**（续行整体不进比对，把判据 3 的续行改成「阈值从 95% 降到 90% 并删掉 gold 校验」照样绿，
    而本轮真正的改动恰好落在续行上）、**H12 留了假绿口子**（末行写着 `FAIL 1`、那 1 就是 H12 的陈旧产物照样能满足它）、
    **H16 只核单向**（本机 SKIP=0 时无事可查）。处置：H14 把续行并进同一条、括号内改动条数改成算出来的；
    H12 的判据本体抽成纯函数 `h12_verdict()` 并加两条（本次除 H12 外零红、产物自记红名单只许是 H12），
    新增 **H12b** 用合成产物直接打这颗（两种合法形状必须绿、其余形状必须红，格数由 `len(_H12_CASES)` 算出、不写进文案）；H16 改成两个方向都核，
    `check()` 登记 `ALL_NAMES` 供反查；A3 补进 `LOCAL_ONLY`（15 → 16 项）；两支跑器对混合换行文件直接拒绝写回。
    **反证读数（P13 判据 2）**：`D:/tmp/ce8.py`（克隆 + 同步工作树 + 拷 `logs/` + 提交基线，正本一字未改）——
    CE-1 只改 P10 判据 3 的**续行** ⇒ **H14 红**；CE-3 往 `LOCAL_ONLY` 塞一条本轮没跑过的死条目 ⇒ **H16 红**；
    CE-4 从清单删掉一项声明 ⇒ 本机（有 `logs/`）形状下 **H16 判绿**，这条**没成立**，照登：正向那一半要靠无 `logs/` 的克隆才咬得住，
    由 `ce6` 的 H16 那条继续承担。H12 的合成反证不靠克隆，直接由 H12b 在审计里跑（`ce9` 那版端到端克隆反证当时被 A2 干扰、
    没能干净复现，所以先换成可判别的纯函数口径，不假装跑过；端到端那一格到本票第 14 条才由 `ce11` 的 `CE-2` 补上）。
    **项数 94 → 95**：本轮只加 H12b 一项；`TAIL_CHECKS` 从 8 改到 9，末尾那条硬断言会盯着这个数当场炸。

14. P13 落盘期（09-12，**不走双轴审查**——① 是落产物第一跑当场撞出来的，② 是第八轮 Standards 轴自查抓出来的）。**账**：新抓 2 条、成立 2 条、驳回 0 条。
    **H12 的调用点没 flush ⇒ 恒红不可收敛**：第七轮把判据本体抽成纯函数之后，`H12` 仍在**就地** `check()`，
    而 `H12b`、`H16` 在它之后才登记，于是送进 `h12_verdict` 的计数比最终少两笔，`exp` 永远对不上，
    「`PASS 95 FAIL 0`」这个状态在数学上不可达，落盘第二跑不可能绿。第六轮的注释里刚写过「拿中间快照比末行是恒红假判据」，
    第七轮只把 H12 挪到「自己之前那一段之后」，没挪到「所有 check 之后」，同一个病换了更隐蔽的形状回来了；
    而 `H12b` 的合成用例写死 `np = N-1`，恰好把这个 off-by-2 盖住，它自己那一格照样判绿。
    **处置**：`check()` 登记走 `defer`/flush（落在 `H16` 之后、末尾硬断言之前），`H16` 反查时把推迟项算成「本轮跑过」，
    `_run_h12` 开头加一条「已登记数 == N-1」的防呆断言，`H12b` 补一格「本次计数少两笔（未 flush 的旧调用点形状）」必须判红，
    把这条调用点不变量钉成可重跑断言；顺带把 `H12b` 名字与说明里硬写的「六」改成 `len(_H12_CASES)` 算出来的
    （和 P13 ① 治的硬写文案同一种病）。**反证**：仓内那一层是新加的那格合成用例（跟着审计一起跑的机器断言）；
    克隆那一层见本条末的「端到端反证补记」。
    **P13 落盘期（`ba95091..fb8b182`）的落盘产物提交共四笔，末行读数逐笔照登**（窗口是第八轮 Standards 轴补的：`git rev-list --count fb8b182 -- 产物` 实为 9 笔，只有加了窗口才是 4）（正本一字不改，逐笔提交 + push；前两笔是收敛过程，后两笔是文档订正后同形状复跑）：第一跑 `汇总：PASS 94  FAIL 1  SKIP 0  共 95 项`、退出码 1，
    唯一红是 H12，detail 打「末行 (94, 0, 0, 94) vs 应为 (95, 0, 0, 95)」（入仓产物仍是 94 项那一版）；
    第二跑 `PASS 95  FAIL 0  SKIP 0  共 95 项`、退出码 0，H12 detail 打「末行 (94, 1, 0, 95) vs 应为 (94, 1, 0, 95)；
    产物里 H12 当时=红」——这正是 `H12b` 里「收敛途中」那一格的真实形状；第三跑复测 `PASS 95 FAIL 0 SKIP 0`、退出码 0、
    工作树 clean，H12 detail 打「末行 (95, 0, 0, 95) vs 应为 (95, 0, 0, 95)」；其后 `22aee78`（文档订正那一笔之后）
    与 `fb8b182`（读数出处改成脚本版本那一笔之后）各复跑一次，末行仍是 `PASS 95 FAIL 0 SKIP 0 共 95 项`、退出码 0、
    工作树 clean——这就是稳态：产物绿着入仓时 H12 判绿、产物落后一笔时 H12 判红，两种形状都实测过。
    **干净克隆复测（P13 判据 3）**：`git clone` 到临时目录、不拷 `logs/`、不提交，跑**最后一次改动审计脚本那一笔**（`299c7e4`，即 `git log -1 --format=%h -- .scratch/shoppilot-mvp/round3-closeout-audit.py` 当下给出的那个 sha）。**这条读数的锚点规矩**：脚本一变就得重跑这一格、并把上面那个 sha 换掉；本句只担保「`11c96a0` 这一版脚本 ＋ 当前文档」的形状，不担保历史每一笔。（先前这里写的是「`c3e7ba8` 起、对到目前为止每一笔都成立」，随后脚本为 ⑨ 改了 13 行，那句当场过期——这是本轮第二次把没过验证的「到处都成立」写进文档，已按实然收回。第八轮闭环又改了三处脚本，锚点按同一条规矩换成 `40ff550` 并重跑，读数字段级不变；第九轮闭环又改了量具（推迟项防呆改扣尚未落地数、红名单改与全名等值、flush 两表同清（那条同长断言沿用 `c3e7ba8`、不是本轮补的，见第 17 条 `S8`）、`H12b` 夹具项数由 `N` 推，外加 `_DEF_LANDED` 挪位那一笔），锚点按同一条规矩换成 `11c96a0` 并重跑，读数字段级不变；第十轮闭环又改了量具（`H12b` 夹具补三格让判据②与③各自承重、两格陈旧计数由 `_FX` 推、夹具注释订正），锚点按同一条规矩换成 `299c7e4` 并重跑，读数字段级不变。）`c3e7ba8`、`1483d57`、`40ff550`、`11c96a0` 与 `299c7e4` 五版的同形状读数字段级相同（`PASS 80  FAIL 0  SKIP 15  共 95 项`、退出码 3、无 traceback、红 0 项），所以后三次重跑都只是把锚点挪回真话，没有读数变化。**照登一处残余风险**：「文档里的 sha 锚点随脚本过期」这类漂移，目前只有这条文字规矩、没有机器断言盯着（H15 盯的是入仓产物记的 HEAD，不是散文的锚点）；再犯第三次就补一格。**这笔承诺的账在第 16 条末算、「犯」的判据在第 17 条写死**：判据是「入仓文档在 commit 那一刻携带的读数当时能否机器重跑」，按这条判据那类「犯」在盘上只有前两笔（都是把没过验证的『到处都成立』写进文档），第九轮那次换锚点不算第三笔，所以那格机械断言仍欠着；为什么这一格不好补、补的话要钉哪三样，写在第 16 条末。13:52 那次反证跑器崩在锚点上属**另一条**欠账（仓外跑器锚点随正本静默过期），记在第 17 条末。
    `汇总：PASS 80  FAIL 0  SKIP 15  共 95 项`、**退出码 3**、无 traceback、红 0 项。声明清单 16 项、实跑 SKIP 15 项，
    少的那一项是 `A3`——本机 `:8082` 网关在跑，它读活体读数判绿、不走 SKIP。
    **顺带订正 P13 判据 3 的原文**：它原先写「实跑 SKIP 恰为这 16 项」，那是把「声明 16 项」当成「克隆必 SKIP 16 项」，
    与 `H16` 正向真正钉的 `⊆` 不符；已按机器读数改成「必须是这 16 项的子集」，P12 判据 3 那句括号里的对照数一并改准。
    **端到端反证补记（`ce11`，现由换代后的 `ce12` 重跑，见本票第 15 条）**：把克隆基线做干净（`-c core.autocrlf=false` 检出免 `D3` 因换行漂移 SKIP、基线提交后
    `git update-ref refs/remotes/origin/main HEAD` 免 `A1` 因多一笔 baseline 提交而假红、拷 `logs/`）之后，克隆里拿到
    **0 红 0 SKIP、退出码 0** 的基线，`H12` 不再走三态，于是第 13 条那句「端到端没能干净复现」可以收紧：
    `CE-2` 往入仓产物的红名单里伪造一条别人的红 ⇒ **H12 红**，端到端真咬住了。另三格专打本条的机制：
    `CE-5a` 把 `defer(...)` 换回就地 `_run_h12()` ⇒ 防呆断言当场 `AssertionError`、退出码 1；
    `CE-5b` 连防呆一起拆 ⇒ `H12` **静默恒红**，正是落盘第一跑撞到的那个形状；
    `CE-6` 把 `H16` 里 `+ DEFERRED_NAMES` 那一半拆掉 ⇒ **H16 红**。`CE-1`/`CE-3` 在当前脚本上复跑仍判红、
    `CE-4` 仍不成立，照登。**项数不变**：仍是 95 项、`TAIL_CHECKS` 仍 9——这两笔只往 `H12b` 的合成表里加用例（第七格、第八格），不新增 check。
    **② `h12_verdict` 的红名单前缀匹配是宽的 ⇒ `H12b` 能冒充「H12 自己那一格」**：`was_red` 用 `"H12" in l`、条件 ③ 用 `f.startswith("H12")`，而跑器里确实有个名字以 `H12b` 开头的 check——往入仓产物的红名单里写一条 `H12b ...`，旧尺子会同时把 `was_red` 骗成「H12 当时是红」并让「红名单只许是 H12 那一格」放行，伪造产物照样判绿（就是第 13 条 ② 想堵的那类假绿，换了个前缀漏进来）。处置：两侧统一收紧成 `H12 ` 前缀（check 名打印时 `H12` 后面就是一个空格），`H12b` 补第八格「红名单写 `H12b`」必须判红。**真反证对拍**（把新旧两把尺子摆在同一份输入上各跑一遍）：旧尺子判绿、新尺子判红；合法那格（红名单写 `H12`）在新尺子下仍判绿，说明收紧没打死收敛途中的形状。`H12b` 那一格的名字随 `len(_H12_CASES)` 自动变成 8，产物里那行就是读数（第九轮又补了「全名 + 后缀」那一格，名字随之自动变 9，见第 16 条）。
    **可复现性的边界照登**：上面这些克隆反证（含 P13 判据 3 那笔不拷 `logs/` 的复测）的跑器（`cleanclone4.py`、`ce11.py`、`ce12.py`、`ce13.py`）按「反证不进被审树」的规矩一直放在仓库外的临时目录里，**不在本仓库内**，所以这几条读数是「同机同环境可复跑」而不是「换台机器 `git clone` 下来一条命令可复跑」；换环境的读者要复现，得照这里写明的形状（克隆 → 覆盖 `.scratch` 与 `README.md` → 按需拷 `logs/` → 基线提交后把 `refs/remotes/origin/main` 追平 → `-c core.autocrlf=false` 检出）自己搭那一步。仓内唯一一条命令可复跑的是收口审计本身：`python -X utf8 .scratch/shoppilot-mvp/round3-closeout-audit.py`。

15. 第八轮双轴审查（09-12，fixed point `ba95091`；Standards 轴 `01a0936e-345b-7682-b12b-3734343dc282` 报 5 条 = 1 硬违规 + 4 judgement、Spec 轴 `01a0934f-f213-7081-9b53-e5189ef7adae` 报 5 条）。**账**：合计**报 10 条**，两轴有 1 条同指一处（「落盘产物提交共四笔」缺窗口），去重后**独立成立 9 条、驳回 0 条**；处置与判据见计划 P14，这里只记本票该收的读数与两处「说过头」。
    **两处「说过头」是本条的正身**（前七轮治的都是硬写的数字，这两轮治的是**注释里对兜底能力的自述**）：
    ① **flush 那句只比数量**，却在注释里写「不会放过真正的死条目」——克隆实测把 `_run_h12` 正常路径那格换成
    `check("H12Q 冒名格", ...)`，数量照样相等、`H16` 照绿、末行 `95/0/0` 退出码 0，`LOCAL_ONLY` 声明的 `H12`
    那一格从没以自己的名字落地过。现补「按名字逐个核」那道 `assert`，注释改成指明真正的闸在哪一道。
    ② **上一轮为堵 `H12b` 冒充而收紧的 `startswith("H12 ")` 把「自己那一格」绑死在名字字符串上**：改 `_H12_NAME`
    之日就是「收敛途中」那格长成恒红假判据之时。现改绑常量（调用期求值），`H12b` 第四格输入也换成 `_H12_NAME` 全名。
    同型的还有 `_run_h12` 开头那条防呆把「本轮只推迟一项」写死成 `== N - 1`，且文案指错方向；现按 `len(DEFERRED_NAMES)` 算。
    **克隆层反证读数（`ce12`，八条）**：基线 **0 红 0 SKIP、退出码 0**，`H14`/`H12`/`H16`/`H12b` 四格全绿；
    `CE-2` 端到端（往入仓产物红名单伪造别人的红）⇒ `H12` 红；`CE-1`（改判据续行）⇒ `H14` 红；
    `CE-3`（清单塞死条目）⇒ `H16` 红；`CE-5a`（拆 `defer`）⇒ 防呆 `AssertionError`、退出码 1、无汇总行；
    `CE-5b`（连防呆一起拆）⇒ `H12` 静默恒红；`CE-6`（拆「推迟项算跑过」那一半）⇒ `H16` 红；
    **`CE-7`（新闸：推迟项冒名）⇒ 按名字核那道当场 `AssertionError`、退出码 1**。
    **`CE-4` 不成立**（从清单删一项声明，本机有 `logs/` 时正向咬不到），照登、不改形状。
    **跑器换代的账**：`ce11` 的锚点在本条 ①② 两处改动、外加末段那条同型防呆（合起来三处改动：`N - 1`、`startswith("H12 ")`、`== N - len(DEFERRED_NAMES)`）之后当场过期，
    所以克隆层反证全部重做到 `ce12` 上重跑；跑器仍在仓外，仍是「同机同环境可复跑」那一档，边界见第 14 条末。
    **零额度这条的读数形状订正**：判据里那句「`tokensUsedToday` 差值 0」原先只由 A3 这一个**时刻**的读数撑着，
    是点读数不是差值。现按可复核形状重钉：**有界**窗口 `9d444c9..16a35fa` 里 **17 份**入仓产物各自记
    `tokensUsedToday=0`（这 17 笔的提交钟点实算最早 `05:29`、最晚 `11:59`——钟点取自提交元数据，17 份产物正文里
    查无 `HH:MM`：原先那半句把提交钟点写成「产物里的时点」，按本票自己立的规矩当场作废。两条逐笔核对命令见计划 P14 ⑰），该计数器单调不减、窗口首末皆 0 ⇒ 区间内任意两点差值为 0。右端写死而不写 `..HEAD`：这个份数每落一次盘
    就涨一笔，写成 `HEAD` 就是把会动的数当读数抄进文档；`A3b`（提交信息里不得出现花钱跑测的痕迹）作离线反证。
    **业务结论的覆盖面按盘订正**：Spec 轴那句「业务证据（`D3`、`rescore`）在干净克隆上必 SKIP」核下来要改——
    `--rescore` 那五格（`D5`–`D9`）读在库 CSV，克隆里判 **PASS**；`D3` 之所以 SKIP 只是默认 `core.autocrlf=true`
    把 226 个文件翻成 CRLF，`-c core.autocrlf=false` 检出即判 **PASS**。真正的边界是 **`logs/` 不入库**那一类
    （`F1c`、`G2b`、`G3`–`G8`、`H1`、`H1b`、`H1d`、`H2`）加要活体网关的 `A3`、与本机全绿产物不同源的 `H12`。

16. 第九轮双轴审查（09-12，fixed point `eb43425`；Standards 轴 `01a093d6-761a-7ce1-888c-7ee983168dcb` 报 5 条 = 2 硬违规 + 3 judgement、Spec 轴 `01a093d6-7cf8-7cc2-80d1-f65dd3a213d8` 报 5 条 = 判据 2、判据 3 各一处 + 判据 4 的三个小问）。**账**：合计**报 10 条**，两轴有 2 条同指两处（本票条号那处、那句悬空续号），去重后**独立成立 8 条、驳回 0 条**；处置与判据见计划 P15，这里只记本票该收的读数。这份账与第 15 条那份「10−1=9+0」不是同一笔：两轮都报 10，去重数与成立数不同，别串成一份。
    **克隆层反证读数（`ce13`，十三条 rec：11 个 `CE-` 号 + 基线格 + `CE-8b` 附带格；12 成立 + `CE-4` 不成立。脚本 `11c96a0` ＋ 本轮订正后的两份文档）**：基线 **0 红 0 SKIP、退出码 0**，`H14`/`H12`/`H16`/`H12b` 四格 `PASS/PASS/PASS/PASS`；`CE-1`（改判据续行）⇒ `H14` 红；`CE-2`（往入仓产物红名单伪造别人的红）⇒ `H12` 红；`CE-3`（清单塞死条目）⇒ `H16` 红；`CE-5a`（拆 `defer`）⇒ 防呆 `AssertionError`、退出码 1（本轮真值：上一笔它炸出来的是 `NameError`，诊断被声明顺序冲掉，见 `bbade2b` 那笔挪位）；`CE-5b`（连防呆一起拆）⇒ `H12` 静默恒红；`CE-6`（拆「推迟项算跑过」那一半）⇒ `H16` 红；`CE-7`（推迟项冒名）⇒ 按名字核那道炸、退出码 1；**`CE-8`／`CE-8b` 对偶** ⇒ 同一形状（第二个推迟项排在 `H12` 之前落地）旧式子当场 `AssertionError`、现行式子不炸且汇总行照打（那个克隆里 `H12` 因产物少一笔而红，是插一格的必然附带，不是防呆失效），新登记的 `CE8` 那格真以自己的名字落地为 `PASS`；**`CE-9`**（把等值匹配换回上一笔的前缀匹配）⇒ `H12b` 判红。**`CE-4` 仍不成立**（从清单删一项声明，本机有 `logs/` 时正向咬不到），与前几轮一样照登、不改形状。
    **尺子换代账**：`ce12` 的锚点在本轮 S1、S3 两处改动之后过期，克隆层反证因此全部重做到 `ce13` 上；对拍器换代到 `D:\tmp\oldvnew4.py`（两把尺子都从正本 `def h12_verdict` 里抠，抠不到当场抛错），实得四格：「红名单写全名」两把都判绿、「全名 + 后缀」现行判红而第八轮那把判绿、`H12b` 与 `H12Q` 两个冒名格两把都判红。跑器仍在仓外，仍是「同机同环境可复跑」那一档，边界见第 14 条末。
    **业务结论这一面没被挤掉**：Spec 轴按当前盘复核过 `docs/adr/0021-*.md` 的结论与钉它的机器格（`D3`、`D5`–`D9`、`E4`/`E4b`）都在产物里判绿，`D7` 仍打 `72.2% -> 94.4%`、`D8` 仍打「差异集合恰 4 条」——九轮量具自审没有把本票的正身换掉。
    **欠着的那格机械断言长什么样（第 14 条末那句承诺的下文）**：想钉的是「散文里那句『最后一次改动审计脚本那一笔（X）』的 X，必须等于当下 `git log -1 --format=%h -- <脚本>` 给的那一笔」。它自带自我指涉：改一次脚本就多出一次「X 还指着上一笔」的红，而 `H12` 现行只认「本次除 H12 外零红」那一种收敛形状，两类红同时存在时盘上没有任何一份产物能自洽。要补这格，得先把 `H12` 的合法形状扩成「允许锚点与它同格红」——那是**放宽**一条已经跟着跑了九轮的判据，本轮不做，照登在案等用户拍。
    **订正一笔自家的计数（第十轮自查，不走双轴审查）**：本条抬头与计划 P15 的判据 2、状态段那三处「`ce13` 十二条」是我抄进文档的旧数——`D:\tmp\ce13.py` 实际打 **13 条 rec**（11 个 `CE-` 号 + 基线格 + `CE-8b` 附带格，12 成立 + `CE-4` 不成立，见 `D:\tmp\ce13b-out.txt` 末尾那段汇总），三处已按实数订正。这仍是 P14 ⑩ 那句「散文里的计数目前没有机器断言盯着」的又一例，所以它一并记在上一段那份欠账里，不算补上。

17. 第十轮双轴审查（09-12，fixed point `dd1bceb`；Standards 轴 `01a093d6-761a-7ce1-888c-7ee983168dcb` 报 4 条 = 1 硬违规 + 3 judgement、Spec 轴 `01a093d6-7cf8-7cc2-80d1-f65dd3a213d8` 报 5 条 = 判据 1 一处小瑕、判据 2 一处归属、判据 3 一处换代做半、判据 5 一处覆盖面、承诺账一处）。**账**：合计**报 9 条**、两轴无同指（一条钉夹具的承重、一条钉夹具的抄死，同一处代码的两个不同缺陷），去重后**独立成立 9 条、驳回 0 条**；处置与判据见计划 P16，这里只记本票该收的读数。这份账与第 15、16 条那两份不是一回事（第八轮 10−1=9+0、第九轮 10−2=8+0、本轮 9−0=9+0）。
    **本轮真补上的一条防线（`S6`，硬违规）**：`H12b` 名字里那句「三条都真能失败」原先只兑了判据①——Standards 轴从正本抠出 `h12_verdict` 做三组变异（摘掉②「末行自洽」、摘掉③「本次除 H12 外零红」、摘掉③「红名单只许是 H12」），旧九格的判定一字不变。处置：`_H12_CASES` 补三格、各钉住一条，夹具 9 格 → 12 格（格数由 `len(_H12_CASES)` 算，`H12b` 的名字自己跟着变成 12）。反证读数（`D:\tmp\ce14.py`，只读正本、不写仓内任何文件）：**七条全成立**——基线 12 格逐格符合预期；M1/M2/M3 各只翻绿本轮新补的那一格、旧九格一字不变；M0（摘掉 `got == exp`）在旧九格里当场翻两格，证明判据①本来就承重；M4（等值换回上一笔的前缀匹配）仍抓到「全名 + 后缀」那格。照登一条边界：`_mk` 恒写 `SKIP 0`、调用点恒传 `n_skip=0`（真跑时 `SKIPS` 非空走三态分支、到不了那颗纯函数），判据①第三个字段在夹具里永久空转，那是调用点不变量、本轮不拿它冒充已钉。
    **另两处量具面订正**：夹具注释那句「产物侧的总数恒为 `N-1`」按实然改口（真产物末行是 `共 95 项`＝`N`，`N-1` 是**此刻已登记的 check 数**，`S7`）；两格故意写歪的陈旧计数改成由 `_FX` 推、不再抄死 `88`/`90`（`W4`，`_FX = 94` 时实得仍是 88 与 90，判定值一字不变）。
    **文档面四处归属订正**：P15 判据 2 里 `ce13` 的 CE-5a 归属改回「执行期补抓」那一格、`S4` 明写没有克隆层反证（`W5`）；P14 段内 `ce12`/`ce13` 混引按 `ce13` 实读对齐，并补一条「ce11→ce12 是第八轮的账、ce12→ce13 是第九轮的账」的分界（`W6`）；「flush 两表同清并加同长断言」里那条 assert 的来历改回 `c3e7ba8`、本轮真正新增的只有 `DEFERRED_NAMES.clear()`（`S8`，替自己记功的一族）；「十二条」按实数订正这笔成立于所审那一笔、由前一笔 `c0050b8` 补上（`S9`）。
    **承诺账的判据写死了（`W8`）**：「犯」＝入仓文档在 commit 那一刻携带的读数当时不能机器重跑（核对方法：该笔提交的钟点 对拍 支撑它的跑器输出文件时间）。按这条核：引用 `ce13` 读数那笔提交在 14:35:45、成功那一跑生成于 14:19:29，两轴后来又各自独立重跑过 ⇒ 第九轮那次换锚点不计第三笔。13:52 那一跑崩在 `CE-2` 锚点上不属「第三笔」这一族，但它暴露的「**仓外反证跑器锚点随正本静默过期、崩掉只留在 stderr、盘上无入仓记录**」按新欠账记在这里，不再用「规定动作」那个分类词带过。
    **现在欠着的四条**：散文里「最后一次改动审计脚本那一笔」的 sha 锚点随脚本过期、无机器断言（补那格要先放宽 `H12` 的合法形状，等用户拍）；散文里硬写的计数（份数／条数／格数／笔数）无机器断言盯；仓外反证跑器的锚点随正本静默过期且无入仓记录；`S`/`W` 标号的引用落点无尺子（`_all_marks` 跨三份文档求并集，跨文档同号也算落点）。
    **业务结论这一面没被挤掉**：本轮只动夹具与文档，`D7` 仍打 `72.2% -> 94.4%`、`D8` 仍打「差异集合恰 4 条」、`E4`/`E4b` 绿，README 那两套读数、8 个 id 与「未达成」一字未动；`ce13` 在本轮脚本 `299c7e4` 之上重跑仍是 12 成立 + `CE-4` 不成立、基线 `红 [] SKIP []`、退出码 0。

**追加追问**

- *Q：改标注是不是为了让数字过线？* A：八个 id、四条问句原话都钉在 README 指标表那一格里，读者可以自己对照 `eval/cases-part2-action.jsonl` 复核；判据原文是分意图各 >= 95%，新判据下仍有 5 行未达线，承诺项结论没变。另外差异集合由 `--rescore-expected-diff` 锁死成恰好那 4 条，多放开一条重算命令就非零退出。
- *Q：95.6% 越过了 95%，为什么还写未达成？* A：因为承诺项的量纲是分意图，不是聚合；聚合那格并列两套读数（93.3% 与 95.6%）正是为了防止它被单独引用成"过了"。
- *Q：重标为什么不重跑？* A：重标只改判据、不改样本采集，`选对工具` 依赖的实际调用链当年已落进明细，重算与实测共用同一个 `judge()`，同源只差采样；参数与 `NOT_FOUND` 离线补不出，前者记成不可比对、后者下沉成 JVM 断言，都不靠嘴说。花钱那条约一键预检命令留在本票「备用的花钱路线」。
- *Q：这套改动怎么保证不是又一次"把测试改成能过"？* A：三类反证都在机器上：把四处量具修复分别改回旧语义，`--selfcheck` 必须红；把坏标注（空合格集、非法工具名、重复元素、越权样本缺标记、标记用跨租户共享词、标记用店名子串/整串/加长串、标记用裸街道名）注入仓库外临时副本，`build_eval_set.py` 必须 `FAIL`；阈值常量与 `EVAL DONE` 由断言盯住未在 diff 中出现。跑一遍 `python scripts/verify_eval_judge.py` 就是这 40 条，一条命令全复现。
- *Q：门禁跑不完的时候，你怎么确定那不是防线失效？* A：看现场而不是看等待时长——第三轮那次 `stack` 步 20 分钟不动，被冻住的 pwsh 是活的、19 线程、CPU 停在 5.546875 不再增长、**没有任何子进程**、`8082`/`8091` 始终不通，而它的日志最后四行是被拉起的第三方进程自己打的 `INFO`；这五条合起来指向"继承句柄的探活挂死"，不指向任何一条业务防线，因为业务防线连开口的机会都没有。同一套区分用在 `polarity` 的 `exit 3` 上：前置不成立时脚本自己拒绝下结论，栈不动单跑一次就绿，那种红也不记成防线失效，见 README「假红」一节。
