# ShopPilot 工程任务书（Project Charter）

**项目名称**：ShopPilot — 面向电商大促的高并发智能客服与业务网关

**架构定位**：融合"静态政策 RAG + 动态微服务 Tool Calling + 多级语义缓存"的工业级智能客服对话网关，解决大促瞬时并发冲击与复杂业务闭环问题。

> 本文件为原始任务书，保留以追溯。经 2026-09-08 grilling 会话细化后的可执行版本见 [PLAN.md](./PLAN.md)，逐条决策理由见 [docs/adr/](./docs/adr/)，术语定义见 [CONTEXT.md](./CONTEXT.md)。任务书中与细化结论冲突之处（缓存前置、同进程反射调用 JPA、QPS 与 embedding 吞吐矛盾、租户未定义）以 PLAN.md 为准。

---

## 核心业务背景与痛点

* **大促流量峰值冲击**：双十一/大促期间海量用户涌入，80% 的咨询为重复热点（"发什么快递"、"满减怎么用"），直穿大模型会导致 API 费用雪崩与推理排队超时。
* **业务动作与知识割裂**：传统客服机器人只能回复干巴巴的 FAQ 文本，无法联动底层业务系统完成"查订单物流"、"修改收货地址"、"申请退款"等实质性动作。
* **语义漂移与缓存污染**：普通文本匹配无法应对口语化提问，而粗糙的语义缓存容易把反义提问（如"怎么退款"与"为什么不给退款"）误判为同一意图。
* **大促服务可用性保障**：外部依赖（物流接口、支付接口、云端 LLM）抖动时，缺少平滑降级与转人工服务兜底机制。

---

## 核心技术栈架构矩阵

| 架构分层 | 核心技术选型 | 关键职责与面试考点 |
| --- | --- | --- |
| **网关与编排核心** | **Java 21 + Spring Boot 3.3+** | 开启 **Virtual Threads**；统一接入鉴权、租户级令牌桶限流、动态意图路由分发。 |
| **业务意图与工具调度** | **自研轻量 Agent 状态机 / Function Calling** | 识别静态规则检索（RAG）与动态交易动作（Tool Use）；参数槽位抽取与微服务 API 聚合。 |
| **多级高吞吐缓存** | **Redis 7.x + Qdrant (1024 维)** | **L1 精确哈希缓存** + **L2 向量语义缓存**（余弦阈值 >= 0.95），拦截 80%+ 重复热点。 |
| **知识检索与规则库** | **Qdrant (稠密向量) + Elasticsearch (倒排)** | 存储平台 7 天无理由、退换货条款、跨店满减规则，执行混合检索并注入 Prompt。 |
| **动态业务 Mock 中台** | **H2 / SQLite + Spring Data JPA** | 模拟高仿真的订单表（Orders）、物流追踪表（Logistics）、优惠券表（Coupons）。 |
| **推流与通信通道** | **Spring MVC `SseEmitter`** | 端到端打字机流式输出，携带 Function Calling 异步执行状态通知。 |

---

## 关键架构指标（SLO / 验收基线）

| 维度 | 指标目标 | 保障手段 |
| --- | --- | --- |
| **热点流量拦截率** | 重复热点问题缓存拦截率 **>= 80%** | Redis 精确匹配 + Qdrant 余弦相似度语义缓存 |
| **端到端首字时延** | 缓存命中 TP99 **< 30ms**；未命中端到端 TTFT **< 500ms** | 内存级缓存穿透保护 + SSE 流式推流 |
| **工具调用准确率** | 订单查询、地址变更参数抽取准确率 **>= 95%** | 结构化 Function Schema 定义与 Prompt 强约束 |
| **大促吞吐极限** | 本地单实例压测 QPS **>= 1200+**（80% 缓存命中） | Java 21 虚拟线程并发调度 + 连接池优化 |
| **业务幂等与安全** | 涉及改地址/退款动作 **100% 幂等** | 引入 Token 防重与订单状态前置校验 |

---

## 里程碑交付计划清单（极速冲刺版）

```
Sprint 1: 业务底座与规则切分 ──> Sprint 2: 动态路由、工具调用与缓存 ──> Sprint 3: 压测与面试成果
```

### Sprint 1：动态业务底座与政策知识库初始化

* [ ] **容器与中间件启动**：编写 `docker-compose.yml`，拉起 Redis 7.0、Qdrant、Elasticsearch（限 512M）。
* [ ] **电商规则知识库准备与向量化**：收集/生成 30 篇电商标准政策 Markdown；运行离线切分脚本，提取 `rule_type`、`applicable_category` 元数据入库（ES + Qdrant 1024 维）。
* [ ] **动态业务 Mock 中台构建**：建立内存数据库表 `orders`、`logistics`；编写标准 Spring Data JPA 仓储层，提供原子操作 `queryOrderDetail`、`queryLogistics`、`modifyDeliveryAddress`、`applyRefund`。

### Sprint 2：意图路由、Function Calling 闭环与双级缓存

* [ ] **L1/L2 双级防打垮缓存体系**：L1 `MD5(TenantId + NormalizedQuery)` 存 Redis；L2 Query 向量化后请求 Qdrant 缓存集合，Cosine Similarity >= 0.95 直接复用。
* [ ] **意图分流与 Function Calling 状态机**：定义标准 OpenAPI 格式的 Tools 描述；纯规则意图路由至混合检索 RAG 链路，业务查询/操作意图触发工具调用并回填结果。
* [ ] **SSE 流式传输与业务事件广播**：暴露 `/api/v1/support/chat/stream`，触发 Tool Calling 时推送 `event: tool_executing` 等内部状态事件。

### Sprint 3：大促防护、高并发压测与量化成果固化

* [ ] **租户级限流与接口防刷**：基于 Redisson 针对用户 ID 与 IP 实现令牌桶限流；写操作增加分布式业务锁。
* [ ] **Locust 大促混合流量压测**：70% 热点政策咨询、15% 动态订单物流查询、15% 冷门长尾咨询；阶梯施压至 500 并发，记录 QPS 拐点、TP99 耗时、LLM Token 节约率。
* [ ] **面试 STAR 话术与材料整理**：总结量化成果。
