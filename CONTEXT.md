# ShopPilot

面向电商大促的智能客服对话网关：把静态政策问答与动态业务办理收敛在同一条对话链路上。

## Language

**意图 (Intent)**:
一次用户诉求被判定出的业务类别，决定后续走政策问答还是业务办理。
_Avoid_: 分类、标签、topic

**政策咨询 (Policy Inquiry)**:
只依赖平台或店铺的规则文本就能回答的诉求，答案对所有满足同一条件的买家一致。
_Avoid_: 静态问题、FAQ

**业务办理 (Action Request)**:
必须读取或修改某个具体买家的数据才能回答或完成的诉求。
_Avoid_: 动态问题、工具请求

**缓存准入 (Cache Admission)**:
判定一次请求是否允许被缓存服务的关卡；不确定即视为不准入。
_Avoid_: cacheable 标志、可缓存判断

**缓存写回资格 (Write-back Eligibility)**:
答案写回缓存之前要过的门。它与**缓存准入**是两道不同的门：准入管「这次请求能不能读缓存」，写回资格管「这条答案配不配进缓存」——七条否决分别是意图不准入、答案空、答案过短、无检索命中、用过工具、已降级、有错误。检索降级时连负缓存都不写，那会把一次抖动固化成批量转人工。
_Avoid_: 写回判定、缓存命中资格（与准入混称）、写回白名单

**动态请求 (Dynamic Request)**:
不准入缓存的请求，因为答案依赖具体买家、订单或实时状态。

**知识库纪元 (Knowledge Epoch)**:
政策知识库的一次整体版本；政策变更即推进纪元，旧纪元产出的答案同时作废。
_Avoid_: 缓存版本号、kb version

**模式迁移 (Schema Migration)**:
`db/migration/` 下一次 DDL 变更的版本单位，管的是表结构。它与管政策知识的**知识库纪元**是两套互不相干的版本轴：纪元推进作废答案，迁移推进只改表结构，两者不互推。迁移只决定表结构怎么产生，**不改变数据的持久性作用域**——库仍是内存库、每次起栈 reseed，「可查工单」的进程生命周期口径不因引入迁移而变。
_Avoid_: 数据迁移（本仓没有数据迁移）、schema 版本、版本升级、把迁移当成持久化承诺

**精确缓存 (L1)**:
按归一化文本的哈希取答案，只拦字面重复，成本近乎为零。`L1` / `L2` 这两个序号在本项目只指缓存两级。

**语义缓存 (L2)**:
按向量相似度取答案，拦的是同一诉求的不同说法，仅在精确缓存未命中后参与。

**串号 (Answer Bleed)**:
一个买家的答案被发送给另一个买家的事故。

**租户 / 店铺 (Tenant / Shop)**:
数据隔离、缓存分区、限流配额与政策归属的单位。全系统只有一个含义，不指平台、不指品牌、不指接入方。
_Avoid_: 商户、商家、品牌、接入方、平台

**平台 (Platform)**:
运营方，不是租户。拥有平台级政策，但不拥有买家在本店的会话数据。

**买家 (Customer)**:
在平台上跨店铺下单的人，身份全局唯一，但在每个租户下的数据互相不可见。
_Avoid_: 用户、客户、会员、User

**政策范围 (Scope)**:
一条政策是平台级还是店铺级。平台级全租户共享同一份，店铺级仅本租户可见。

**租户隔离 (Tenant Isolation)**:
共享存储下按归属切分可见范围；任何跨租户读取都算越权事故，而非数据不一致。

**会话 (Conversation)**:
一个买家在一家店铺内的一段连续对话上下文，含已发生的轮次与尚未办完的动作。归属单位是「店铺 + 买家」两者，只按店铺归属不构成归属——同店铺内两个买家共用一段会话即是本表上的**串号**，而非上下文继承。
_Avoid_: 对话、聊天记录、session（口语可用，文档统一叫会话）

**待办动作 (Pending Action)**:
会话上已经认下、因信息不全而尚未执行的那个动作，连同已收集到的参数。买家的下一次输入是用来补全它的，不是新诉求。它随会话归属，因此不跨买家续办。
_Avoid_: 中间态、挂起请求、pending tool（口语可用，文档统一叫待办动作）

**槽位 (Slot)**:
一次业务办理必须凑齐的参数（订单号、收件人手机号等）。缺槽位时的正确动作是追问一次，仍缺则转人工——**绝不猜**。只对「正则能确定判缺」的槽位由网关主动追问，其余交给模型抽，免得把用户已经写出来的地址再问一遍。
_Avoid_: 参数、字段、argument（口语可用，文档统一叫槽位）

**计划 (Plan)**:
一次业务办理里按序执行的工具步骤，长度 ≤ 2，每步 = 工具名 + 参数，参数可引用前步结果字段（形如 `{steps[0].result.orderId}`）；前步失败即中止整条计划。它与**工具轮次**上限是同一条边界的两种表述——步骤数不是独立校验出来的，就是工具循环的上界（ADR 0036）。不并行、不分叉、不重排。
_Avoid_: 任务树、DAG、多智能体编排、把"计划"当成模型自由生成的步骤列表

**降级 (Fallback)**:
系统在办不成时给出的确定性出口：不再尝试，给用户一句实话并落一张**可查工单**。原因集合是枚举的——当前 10 个取值，其中**主动转人工（`USER_REQUESTED`）不算降级**，故「降级」= 枚举 − 1 = 9。枚举 10 / 降级 9 / 脚本确定性表 7 行是三个不同的集合、不是互相矛盾，换算规则见 `README.md` 的验收对照段。
_Avoid_: 兜底、异常处理、error handling、把「转人工」当降级、用「8 类降级」这种没有换算依据的说法

**身份领取 (Identity Minting)**:
系统按请求直接给出租户与买家身份，不核验任何真实凭据。它与**身份伪造**是两件事：伪造是没有密钥却签得出合法凭据，领取是有人替你合法地签。本项目的身份防线钉的是前者。
_Avoid_: 伪造（两者不得混称）、假 token、mock 用户

**身份伪造 (Identity Forgery)**:
未经签发而自行造出能通过验签的凭据。它是攻击面，不是使用方式。
_Avoid_: 越权、冒用（冒用是领取或伪造成功之后的后果，另是一件事）

**运维端点 (Ops Endpoint)**:
能改变运行时行为或改掉既有状态的端点，须持运维凭证。凭证证明的是「允许你动」，不证明身份，身份永远另走 JWT。
_Avoid_: 管理端点、后台接口、ops 接口（口语可用，文档统一叫运维端点）

**观测面 (Telemetry Surface)**:
只读、无副作用、不需要身份就能问出运行时事实的那类出口。它与运维端点的区别在有没有副作用，不在要不要凭证——本仓这两轴的对应关系并不齐，别拿一轴推另一轴。
_Avoid_: 探针（探针专指 K8s 的 liveness 与 readiness）、诊断接口

## 意图清单

意图是缓存分区、工具裁剪、限流统计与评测集共用的唯一枚举，新增意图需同步这四处。

**POLICY_RETURN**:
退换货政策咨询，含 7 天无理由与退换细则。

**POLICY_SHIPPING**:
发货时效与快递政策咨询，如发什么快递、多久发货。

**POLICY_PROMO**:
满减、定金膨胀、优惠券等促销规则咨询。

**POLICY_FRESH**:
生鲜保鲜与破损理赔政策咨询。

**ACTION_ORDER**:
查询某个具体订单的状态与明细。

**ACTION_LOGISTICS**:
查询某个具体订单的物流轨迹。

**订单进度 (Order Progress)**:
买家想知道一笔订单离到手还差几步，不指定该由订单状态还是物流轨迹来回答。它是横跨 `ACTION_ORDER` 与 `ACTION_LOGISTICS` 的诉求，不是第三个意图。
_Avoid_: 模糊问句、边界样本、进度查询

**ACTION_ADDRESS**:
修改某个具体订单的收货地址。

**ACTION_REFUND**:
发起退款申请。

**ESCALATE**:
转人工，系统能力边界之外的显式出口，不是失败。

**UNKNOWN**:
判定不出的诉求，fail-closed，不进缓存。

**动作优先 (Action Wins)**:
一句话同时含政策咨询与业务办理时按业务办理处理，因为政策答案被禁止断言个性化结论。

**对偶矛盾 (Contradictory Pair)**:
评测集里两条同租户、同买家、同场景、问法同属一个进度词形（`到哪` / `发了没` / `签收` 这类在订单详情与物流轨迹之间本就等价的说法）的样本，被标成互斥的合格答案。它是认定「gold 标注边界过窄」的唯一合格证据；没有对偶矛盾可引的失分一律算真错。
词形是判据，订单号不是：本轮 4 组里只有 1 组两侧同单号（`ACT-ORD-09` ↔ `ACT-LOG-09` 都是 90002），1 组两侧都还没给出单号（`ACT-ORD-11` ↔ `ACT-LOG-11`），另 2 组两侧单号不同（查不到单与跨租户探单，各自要探的东西本来就不同）。收敛这条定义的是 `scripts/verify_eval_judge.py` 的 `dual_partners()`，不是任何一份文档里的手感描述。
_Avoid_: 标注歧义、边界 case

**可查工单 (Queryable Ticket)**:
转人工链路上留下的、能按工单号从工单系统问出来的单。「可查」在本仓的作用域是**业务 Mock 进程的生命周期**：看门狗自愈重启会全量 reseed、把已落库工单一并抹掉，而这 reseed 与压测和三条演示的「每次起干净世界」是同一条复位语义的两端，不是实现疏漏。跨重启可查与「RESOLVED 之后买家被告知结果」均不承诺；重开条件见 ADR 0030 第 2 条。
_Avoid_: 落库即持久（落库只保证进程内可查）、工单回流（现状不存在此路径，别拿这个词指称它）

## 知识侧

**政策文档 (Policy Document)**:
一份人写的规则原文，如《生鲜破损理赔细则》。政策文档本身不被检索。
_Avoid_: 知识库、文章

**规则块 (Rule Chunk)**:
政策文档切分出的最小检索与引用单元，携带 `ruleType`、`applicableCategory`、`scope` 归属。
_Avoid_: 片段、chunk（口语可用，文档统一叫规则块）

**引用 (Citation)**:
一条答案所依据的规则块集合。缓存条目必须随带引用，否则政策变更时无法判定该答案是否仍成立。

**复核队列 (Review Queue)**:
被点踩的答案进的那条人工队列，`review_status` 从 `PENDING` 单向走到 `REVIEWED`。**复核本身不触发任何自动改写**（ADR 0039 划的边界）；「复核 → 知识库修订 → **知识库纪元**递增」那条链的自动咬合尚未实现，触发条件见 ADR 0039。
_Avoid_: 待办列表、工单队列（工单与复核队列是两张表、两件事）
